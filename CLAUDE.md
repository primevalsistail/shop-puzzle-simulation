# AI-DLC の進め方

既存のAI-DLCスキルを使った調査・設計・質問・実装・テストを継続する。
設計上の疑問や選択肢を出すこともClaudeの役割であり、省略しない。

- AI-DLCの質問票、チェックポイント、設計検討を維持する。
- 質問には、何を決めるか・なぜ今必要か・選択肢・推奨理由・影響・参照資料を含める。
- 大きな設計変更や仕様変更は、提案と影響を先に返す。
- 質問の原文と回答者を残す。
- 完了報告には変更範囲、実行したテストと結果、未実施の確認、残る疑問を含める。

## そのサイクルのスコープ外はissue化する

サイクルの途中で「これはこのサイクルでは扱わない」と判断したものは、**その場でGitHub issueにする。**
「スコープ外」「別件」「後で」と文書に書いて置いたままにしない。
文書の中の「後で」は誰も拾わない。issueだけがサイクルの外まで残る。

- issueにしたら、**そう判断した場所**（計画ファイル・質問票・`audit.md`）に**issue番号を書き戻す。**
  番号が無いと、後から読んだ人はその判断が生きているのか流れたのか分からない。
- サイクルを締めるときは、**「スコープ外」と書いた箇所すべてにissue番号が付いているか**を確認する。
  付いていないものが残っているなら、まだ締めない。
- スコープ外と判断した理由も1行書く。理由が書けないなら、それはスコープ外ではなく未判断である。

先例はCycle 4で、持ち越した歪み7件をすべてissue化して締めた（#30・#31・#35〜#39）。

## mainに反映したらissueをクローズする

実装が`main`に入った時点でcloseする。
**ブランチにあるだけ・ローカルでテストが通っただけでは閉じない。**`main`が反映済みの唯一の基準。

- closeのコメントに、**反映したコミットのハッシュ**と**実装した内容**を書く。
  issue本文と実装が違うなら、**何をどう変えたか**も書く。
- **残作業があるなら、先に別issueへ切り出してから閉じる。**番号をコメントに書く。
  残作業を抱えたままissueを開いておかない。開いたままだと「まだ何も出来ていない」と読まれる。
- 閉じられない理由があるなら、それをコメントに書く（判断待ち・前提が変わって再測定が要る、など）。

**放っておくと実装がissueを追い越す。**2026-09-11時点で、実装済みなのに開いたままのissueが3件
（#18・#25・#42）あり、さらに#35・#36は**本文の数字が実装より古い前提で書かれていた**。
そうなると、issue一覧が現在地を示さなくなる。

## 起動で読むものを絞る

**AI-DLC の起動は固定費が大きい**（実測 2026-09-12）。読むものを名指しで絞る。

- `core-workflow.md`（**23KB ≒ 5,900トークン**）は必読。ここは削れない
- ⚠ **`common/` は10ファイルあるが、読むのは core-workflow が名指しする4つだけ**
  （`process-overview` ／ `session-continuity` ／ `content-validation` ／ `question-format-guide`）。
  **全部読むと 64KB ≒ 16,000トークン。**残り6つは名指しされていない
- ⚠ **`audit.md` は末尾だけ読む。**全部読む場面は無い。
  束F より前は `audit-archive.md`（**97KB**）に移してある
- **現在地は `aidlc-state.md` だけに書く。**ほかの資料を読まないと現在地が分からない状態にしない

# Claude内での司令塔とサブへの委任

ユーザーから受けた依頼を、mainのClaudeが司令塔として受け止める。
サブClaudeが上げた疑問はmainが引き受け、ユーザーに渡すのはPO判断だけにする。

## 委任の前に目的を書く

- 実装に入る前に、対象の計画ファイル冒頭へ、目的・決定済みの前提と出典・今回決めないこと・
  受入条件を書く。サブへ委任する場合もmainが自分で実装する場合も、ここを省かない。
- サブへは要約ではなくファイルパスを渡す。目的、受入条件、触ってよい範囲、触らない範囲。

## サブからの質問の捌き方

サブは不明点があれば実装せず、判断が必要な点を列挙して返す。
mainは各質問を次の4区分のどれかに必ず割り当て、「判断できない」を既定にしない。

| 区分 | 判定 | 回答者 |
|---|---|---|
| 既決 | `audit.md`、`inception/design-log/`、質問票、過去コミットに決定がある | main（出典を示す） |
| 調査 | コード・テスト・データを読めば分かる | main（実測値を添える） |
| 技術判断 | 実装方式の選択で、プレイヤー体験は変わらない | main（理由を記録） |
| PO判断 | 仕様・UX・ゲームデザイン・優先順位が変わる | ユーザー |

技術判断とPO判断の線引きは、その選択でプレイヤーの体験が変わるかどうか。
変わるならPO判断とし、なぜ体験が変わるかを1行書く。書けないなら技術判断である。
出典も実測値も書けない回答は推測なので、mainで確定せずPO判断へ回す。

## ユーザーへ上げるもの

- PO判断は質問ごとに中断せず、サイクル末にまとめて1回出す。
- AI-DLCスキルが作るステージの質問票は、ユーザーが答える。mainが代筆しない。
  聞く必要をなくすのは、代筆ではなく目的と既決事項を先に明文化することで行う。
