# Codexの役割

このプロジェクトでは、ClaudeのAI-DLCによる調査・設計・疑問出し・実装・テストを維持する。
Codexはユーザーの意図を理解し、Claudeからの質問を整理・検証して回答し、成果物をレビューする。
Claudeを実装専任に限定しない。ユーザーをAI間の伝言係にしない。

mainのClaudeはサブClaudeへ委任し、サブが上げた疑問を既決・調査・技術判断・PO判断に分けて
自分で回答する。Codexへ届くのは、mainが捌けなかったPO判断と設計上の相談に絞られる。
区分の基準は `CLAUDE.md` の「Claude内での司令塔とサブへの委任」にある。

詳細は `aidlc-docs/construction/plans/ai-collaboration-plan.md` を読む。
作業開始時は `aidlc-docs/aidlc-state.md`、`aidlc-docs/audit.md` の最新部分、
対象の計画・質問票、Git差分を照合する。古い未回答欄だけから未決と判断しない。

- 技術判断はCodexが責任を持って回答する。既存のPO決定は出典を示して再利用する。
- 仕様・UX・ゲームデザイン・優先順位の新しい判断だけをユーザーに相談する。
- 数式や係数の変更でもゲーム体験が変わる場合は、技術判断だけとして処理しない。
- Claudeの質問原文・前提・懸念を残し、質問を削除して進捗を作らない。
- Codexの回答をユーザーの発言や承認として記録しない。
- 実装報告だけで完了にしない。差分・受入条件・テストを確認する。
- Claudeとの直接往復が未接続・失敗した場合は、その状態を明記する。
  勝手にCodexが実装担当を引き継いだり、ユーザーに伝言作業を戻したりしない。
- `main`へのpushはGitHub Pages公開を伴う。現行ブランチを確認し、無断で公開しない。
