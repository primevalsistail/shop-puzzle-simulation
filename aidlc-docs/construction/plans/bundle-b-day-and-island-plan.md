# 束B — 日と島の骨格（#2 #4 #44 #41 #43）

**選定**: [issue-grouping-questions.md](../../inception/plans/issue-grouping-questions.md) Q1 = **B（束B）**
**資料**: [issue-grouping.md](../../inception/plans/issue-grouping.md) の「束B」

## 目的

**「四つの島を10日ごとに巡る店」を、世界観の文章からコードへ移す。**

いま現在地は `WorldState` にハルヴェラで**固定**されている（#30 で仮に置いたもの）。
そのため需要表4行のうち**1行しか効いていない**。島が動けば4行とも効く。

## 決定済みの前提と出典

| 前提 | 出典 |
|---|---|
| 四島・**10日ごと**・**順序固定**・自分では選べない | [#2](https://github.com/primevalsistail/shop-puzzle-simulation/issues/2) ／ `world.md` §3 |
| **寄港10日 ＋ 航海1日 ＝ 11日で1島**（4島一周 44日） | [質問票](bundle-b-questions.md) Q1 |
| 島名は**正式名のみ**。**季節名を UI に出さない**（「春島」等は存在しない） | #2「島名の扱い（確定）」 |
| 巡る順 = ハルヴェラ → リナツィア → ノアキータ → ミフユリア | `src/taxonomy/islands.ts` `ROUTE` |
| 1島あたり **10日**（`DAYS_PER_PORT`） | 同上 |
| 一日の区分 = 6-10 作業／10-20 営業／20-24 作業／24-6 睡眠 | #25 ／ `TimeManager` |
| 加工はゲーム内時間を消費し、**その間は客が来ない** | #25 Q1=A・Q2=B |
| 現在地と累計販売数は `WorldState` が持つ | #30 |

## 今回決めないこと

- **数値バランス**（#35 加工時間・#36 買値）→ 束D
- **島ごとの需要差・客の構成**（#3 #8 #38 #39 #11 #12）→ 束C。
  需要表4行の倍率が全部 1.3 のままであることは **#39** が持つ
- **クリア判定**（#26 累計売上 → 所持金）→ 束E
- **条件言語の拡張**（#31 #37）→ 束J

## ⚠ 着手前に測ったこと — **目標は1島目で達成される**

`GameService` を実コードのまま回した（6×5 の初期グリッド・営業10時間/日・乱数固定）。

| 条件 | 1日の売上 | 100万レンまで |
|---|---|---|
| ハルヴェラで買える18品を並べ、**在庫を切らさない** | ¥128,831 | **8日** |
| 同じ18品を **999個ずつ置いて補充しない**（売り切るまで） | ¥76,701 | **13日相当**（9日で¥690,309） |
| tier2 を売値順に並べ、在庫を切らさない | ¥502,358 | **2日** |

**つまり 10日周期を入れても、プレイヤーはリナツィアに着く前に目標へ届く。**
巡回は「入れたが体験されない機能」になる。

**原因は束Bではない。**客の判定が「1分ごとに30%で客が来て、**その客が全区画を独立に判定する**」
ためで、区画15なら1人の客が最大15個買う。これは **#8・#11（束C）** が持っている話。

⚠ この測定には前提がある — 在庫は #42 のテスト用設定（全品10000個）に近い状態を仮定し、
**仕入れ・加工の手間を勘定に入れていない。**実際の遊びではもっとかかる。
それでも**桁として10日周期に収まってしまう**ことは変わらない。

**⚠ 加工を入れて測り直した（2026-09-11）: 所持金100万まで 23日 → **20日**。**
初出の測定も最初の訂正も**加工を一度もしていなかった**（素材を買って生のまま売るだけ）。
時計どおりに回して朝の作業時間帯で加工すると 20日（売上¥1,862,716 ／ 仕入れ¥902,419）。
**⚠ 加工時間は営業時間を1分も食っていない。**作業時間帯が 480分/日（朝240＋夜240）、
航海日は 1,080分あり、そこに収めれば売る機会を失わないため。
**これ自体が別の問題**として **issue #53**（加工がコストになっていない）へ。


## 受入条件

1. `npx vitest run` 全件パス ／ `npx tsc --noEmit` ／ `npm run build`
2. **Day11 が航海日、Day12 にリナツィアへ着く**（Q1 = 寄港10＋航海1 に合わせて書き換えた。
   初版は航海日なしを仮定して「11日目にリナツィア」と書いていた）
3. `WorldState` の現在地固定が外れ、**需要表4行が島に応じて切り替わる**
4. HUD に**島名と日付と残り日数**が出る（#44）。**季節名は出さない**
5. 開始時刻が **6:00**（#41）
6. 1日を終わらせるショートカットが効く（#43）
7. **⚠ 満たせないもの**: 実機での目視確認（この環境ではブラウザが動かない）→ #47 と同じ制約

---

## 結果（2026-09-11）

**181テスト全件パス**（着手前 172）／ `tsc --noEmit` ／ `npm run build` 成功。

| 受入条件 | 結果 |
|---|---|
| 1. テスト・型・ビルド | ✅ 181件 |
| 2. Day11 航海 / Day12 リナツィア | ✅ `WorldState.test.ts` が Day1・10・11・12・23・34・45 と航海日の並びを検査 |
| 3. 現在地の固定が外れた | ✅ `getState().現在地` が日付から決まる。仕入れ品が島ごとに変わることも検査 |
| 4. HUD の表示 | ✅ `Day N` ／ `ハルヴェラ島 あと3日` ／ 航海中は `航海中 → リナツィア島`。パネル高 108 → 126 |
| 5. 開始 6:00 | ✅ `1c93c70` |
| 6. ショートカット | ✅ `N` キー。`skipMinutes` を使うので**飛ばした分に客は来ない** |
| 7. 実機確認 | ❌ **未了**（環境の制約） |

**実装したもの**

- `WorldState` が**日付を持ち、現在地をそこから導出する**。保存は日付だけでよい（`SaveData` に追加なし）
- **航海中は店が開かない** — `GameScene` が `isOpen() && !isAtSea()` を渡す。
  仕入れメニューも開けない（島の商人がいない）
- 島を出た／着いたときにメッセージを出す。島が変わったら開いている仕入れメニューを閉じる

**スコープ外にしたもの（issue 化済み）**

| 判断 | 理由 | issue |
|---|---|---|
| 目標に届くのが20〜23日で、ミフユリアに着かない件を直さない | 原因は巡回ではなく客の判定（束C の #8・#11） | [#52](https://github.com/primevalsistail/shop-puzzle-simulation/issues/52) |
| 加工が営業時間を食わず、加工時間がコストになっていない | #25 の意図が時間帯をずらすだけで無効化できる。直すのは束D の判断 | [#53](https://github.com/primevalsistail/shop-puzzle-simulation/issues/53) |
| 需要表4行の倍率が全部 1.3 のまま | 島は動くようになったが倍率は Phase 3 のまま | [#39](https://github.com/primevalsistail/shop-puzzle-simulation/issues/39) |
| 実機確認 | この環境ではブラウザが動かない | [#47](https://github.com/primevalsistail/shop-puzzle-simulation/issues/47) |
