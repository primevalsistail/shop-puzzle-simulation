# 160〜200日で攻略できるようにバランスを見直す

**作成**: 2026-09-15 ／ main
**引き取る issue**: #54（利益率）／ #61（一括で測り直す）／ #63（産地と売れる島の重なり）

---

## 目的

**どの遊び方でも、ゲーム内 160〜200日で目標額 2,000,000 に届くようにする。**

⚠ **値段の2層構造と、層の中の優先順位を崩さずに行う。**

---

## 決定済みの前提（議論の対象ではない）

### 方針（[what-players-should-do.md](../../inception/design-log/what-players-should-do.md)）

| | |
|---|---|
| **1** | ゲーム内 **200日程度**。最短120日、**理想 160〜200日**（PO 再確認 2026-09-15） |
| **2** | 目標金額は**切りのいい数字**。→ **2,000,000 は動かさない** |
| **4** | 目標到達時点で**最大拡張・レシピ全開放でなくてよい。買い残しがあるのが正常**<br>⚠ **改装費の総額 2,236,000 が目標額 2,000,000 を超えているのは、方針どおり。下げない** |
| **6** | **島ごとに需要を変えて取捨選択を繰り返させる。**「こっちで売る／そのためにあっちで買う」 |

### 値段の2層（[questions-tier-in-price.md](../../sessions/questions-tier-in-price.md)。PO 原文「**2重計上しないでください**」）

| 層 | 中身 | ⚠ |
|---|---|---|
| **層1 物の基準値**（品そのもの） | **材料費 ／ 升数 ／ 格** | **加工時間を入れない**（ペルソナ4人全員が反対）<br>**tier を入れない。升数だけに持たせる**（深さは材料費がもう運んでいる） |
| **層2 場と置き方** | **島の需要 ／ 配置ボーナス** | **層1に混ぜない**（PO「需要とボーナスは物の基準値とは切るべき」） |

**優先順位**: **需要のある島 ＞ 配置ボーナス ＞ 加工時間**（PO 2026-09-15）。
⚠ **加工時間は最下位 ＝ 値段に効かせない。**実装は既にそうなっている
（[derive.ts](../../../src/taxonomy/derive.ts) `加工利益 = PROFIT_SCALE × 升数^CELL_EXPONENT × 格の倍率`）。

### そのほか（過去の決定）

- ⚠ **`PURCHASE_RATE` を tier ごとに分けない。**一本だから
  `作る利益 − 転売利益 = 加工利益 × 買値率` が成り立つ
- ⚠ **合わない品を「売れにくく」しない**（PO「消極的な案を取らせたくない」）
- ⚠ **極端な数値調整を避ける**（需要の4行を `1.15/1.40/1.75/1.45` と開く案は却下済み）
- ⚠ **効き目に上限を足して抑えない**（店全体ぶんは区画数で割った平均にする）

---

## 今回決めないこと

- **目標額・日数**（方針1・2で確定）
- **改装費**（方針4）
- **値段の層の構成**（どの要素をどちらの層に置くか）
- **加工が営業時間を削る仕組みそのもの**（#109 で「知らせる」に決着。効き目は測り直しの対象だが、仕組みは触らない）

---

## 受入条件

1. **5通りの方針すべてで、目標 2,000,000 到達が 160〜200日に入る**
   （`全部転売` ／ `浅い品だけ作る` ／ `深い品を作る` ／ `取り合わせを狙う` ／ 昼も加工する版）
2. **層2の優先順位が守られている**: **島の需要による差 ＞ 配置ボーナスによる差**
   （同じ棚を「合う島」と「合わない島」で回した差 ＞ 「狙った並べ方」と「狙わない並べ方」の差）
3. **層1に加工時間と tier が入っていない**（`derive.ts` を読んで確認。既に満たしている）
4. **升数の検査**: 「大きい品だけで埋めた盤面」より「隙間を小さい品で埋めた盤面」のほうが総額が高い
5. **`作る > 転売` が全レシピで成り立つ**
6. **850テスト全件パス ／ `npx tsc --noEmit` 通過**

---

## 手順

- [ ] **1. 現状を測る**（`npx vite-node src/sim/run.ts -- --days=400`。5方針・複数の種）
      到達日・内訳（転売／加工／納品）・層2の効き目の大きさを出す
- [ ] **2. #63 —— 層2の1位の空振りを直す**
      産地＝売れる島が **14/48（29%）、ハルヴェラは 8/12（67%）**。
      ⚠ **方針6 が根拠。恣意的な振り直しにしない**
- [ ] **3. 層2の倍率を、優先順位が立つように置き直す**
      島の需要（`DEMAND_TABLE` 4行）と配置ボーナス（`SET_RULES` の倍率）
- [ ] **4. 層1の水準を、到達日が帯に入るように置く**
      `PROFIT_SCALE` ／ `CELL_EXPONENT` ／ 格の3値
- [ ] **5. 受入条件6つで確かめる**
- [ ] **6. issue 本文を現状に合わせ、close する**

---

## 着手前に分かっている、issue 本文の古さ

| issue | 古い箇所 |
|---|---|
| **#54** | `TIER1_PURCHASE_RATE = 0.5` は無い（いま `PURCHASE_RATE = 0.7` 一本＋産地割引）／ 加工利益の式が「所要分^0.6」のまま（いまは升数ベース）／ 来店30%・購入40/28/18%（いまは 0.15 / 0.06）<br>⚠ **症状1「tier1 一律を直す」は決定により対象外**（率を分けない） |
| **#61** | 目標額 1000万（いま200万）／ 解禁の間隔「4日に1枠」は #111 で消滅 ／ `PROFIT_EXPONENT` は無い ／ #52 は close 済み |
| **#63** | **古い箇所なし。**実測で裏が取れた |
| **#51** | 全品10000個スタートは無い（3品×15個）／ 上限は 999 に戻っている ／ 1日送りは実装済み<br>→ **実際に残るのは `src/debug/` の7キーのみ** |
| **#47** | 絵文字の豆腐（#69 で描いた絵に置換済み）／ 「135品」→ 161品 |
