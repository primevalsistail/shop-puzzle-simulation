# #89 金属と粘土で開く品 — 計画

**作成**: 2026-09-14 ／ **main が書く。実装はサブセッション**
**着手前調査**: #89 は 2026-09-14 に main 自身が立てた issue。本文と実装に差は無い。

---

## 目的

**#87 で鉄鉱石が入り、粘土を足せない理由も外れた。開いた品を実際に作る。**

`crafted-goods.md` §7 が挙げていた3つの限界のうち、**2つがここで外れる**:
「切る道具は金属が無いので全滅」／「器・鍋は粘土が無いので作れない」。

---

## 決定済みの前提（出典つき。サブはここを問い直さない）

| # | 前提 | 出典 |
|---|---|---|
| 1 | **粘土を足す。産地は ハルヴェラ島** | PO 判断 2026-09-14（Q6=C）／ 島は `island-goods.md` §2-B に沿って main が指名 |
| 2 | **金属で開くもの（包丁・はさみ・鍋・釘・金具）と、粘土で開くもの（皿・壺など）を作る** | PO 判断（Q6=C） |
| 3 | **軸は5本のまま。`ItemDef` に欄を足さない** | `axes.ts` ／ INV-3・INV-5 |
| 4 | **加工品の産地は `なし`** | `island-goods.md` §5 |
| 5 | **売値は手で書かない。**tier2以上は `derive.ts` が材料から積む | `recipes.ts` 冒頭 ／ INV-3 |
| 6 | **`display.reading` はひらがなだけ。全品必須** | `invariants.test.ts` |
| 7 | **`durationMinutes` の上限は 480分。**さらに tier ごとに上限がある | `recipes.ts` 冒頭 ／ `depth.test.ts` |
| 8 | **「器」を品名にしない** | ⚠ `island-goods.md` §0-B「複数の品を含む語を品名にしない」。**皿・壺は可** |
| 9 | **鉄・鉄鉱石・松の薪は #87 で入っている。**作り直さない | `items.ts` |

---

## 今回決めないこと

| | 行き先 |
|---|---|
| 足した品の値（basePrice・所要分・出力数）が釣り合っているか | **#61** |
| 色が既存品と見分けられるか | **#56** |
| 絵 | **#15** |

---

## 触ってよい範囲

- `src/taxonomy/items.ts` ／ `recipes.ts`
- 品数・レシピ数を持っているテストとコメント（前回 #87 で触った箇所と同じ。`git log -1 8cb96fd --stat` で分かる）
- `aidlc-docs/inception/worldbuilding/crafted-goods.md` §0 の数の表と **§7**
  （⚠ **§7 の「金属が無い」「粘土が無い」は、この作業で成り立たなくなる。書き換える**）

## 触らない範囲

- `src/taxonomy/axes.ts`（型）／ `rules.ts`（規則データ）／ `derive.ts` ／ `evaluate.ts`
- UI 全般（コメント中の品数の記述を除く）
- `aidlc-state.md` ／ `audit.md` —— main が書く
- 他の品の定義（INV-5）

---

## 受入条件

1. `npx vitest run` 全件パス ／ `npx tsc --noEmit` ／ `npm run build`
2. **足した品すべてについて `island-goods.md` §0 の A・B・C・D を当てて説明できる**
   —— 特に **B の2段判定**（既存154品のどれかを包含していないか／していたら軸が違うか）
3. **INV-1** —— 既存154品の全属性（導出値を含む）が動いていないこと
4. `rules.ts` を1行も触っていないこと
5. **`crafted-goods.md` §7 が実装に追い越されたままになっていないこと**
