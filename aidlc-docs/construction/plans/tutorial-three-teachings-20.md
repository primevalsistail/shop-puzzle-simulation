# 遊び方の案内に3つ足す（#20 の後半）

**作成**: 2026-09-15 ／ **指示**: PO「#20：対応して」（2026-09-15）

---

## 目的

**#20 の本文下半分にある3つのチェックを埋める。**掛け合い化そのものは `16a3493` で済んでいる。
3つとも**いま画面のどこにも出ていない**（常設の字では言わないと決めたため）。

## 決定済みの前提（議論の対象ではない）

| | 決定 | 出典 |
|---|---|---|
| **常設の字を足して解かない** | **最初の一度で教える** | #20 本文（PO 2026-09-13） |
| **説明は猫がしない** | 猫は指すだけ。中身はノエラ | #20 本文・`tutorialSteps.ts` |
| **字で凡例を出さない** | 行の色の意味も、案内で言う | #20 本文 |
| **1行は面の幅に収める** | `layout.test.ts` が見ている | `tutorialSteps.ts` |

## 今回決めないこと

- **マイセットの `セーブ` / `ロード` の言い換え**（→ #79 で拾う。#75 のクローズコメント）
- **#79 の画面の情報量**。案内の外は触らない

## やること

**段を4つから6つにする。**⚠ **1つの段に押し込むと、面（720×420）に4行しか入らないので溢れる。**

| 段 | 中身 |
|---|---|
| 5（新）**棚の色** | 緑＝この島の産でその品だけ安い ／ 白＝どの島でも買える ／ くすんだ行＝まだ買えない ／ **島を移ると色が変わる** |
| 6（新）**いつ戻るか** | 10日で出る ／ **戻るのは40日後** ／ **あいだに3島を回る**（40の中に10が入っている） ／ **知らないうちは材料を買う** |

⚠ **issue 本文の「青＝どの島でも買える」は実装と違う。**`palette.ts` の `ROW_ANY` は `0xefeee4`（生成り）で、
**青の行は無い。**見たままの言葉（白い行）で書く。

## 受入条件

1. **案内が6段になり、5段目に行の色・6段目に40日と序盤の買い物が出る**
2. **どの行も面の幅に収まる／猫の行はノエラの行より短い**（既存テスト）
3. **`npx vitest run` 全件パス ／ `npx tsc --noEmit` ／ `npm run build`**
