# 段階1「機能作成」—— 計画

**作成**: 2026-09-12 ／ main
**依頼（原文）**: 「AIDLC、機能追加をまとめてやりたい。
私はある程度の方向性は答えるので主導はmainとサブセッションで行ってほしい。」

⚠ **この文書は着手前に書く。**目的・決定済みの前提・今回決めないこと・受入条件を先に固定し、
サブへの委任も自分で書く実装も、ここから外れないようにする（`CLAUDE.md`「委任の前に目的を書く」）。

---

## 目的

**[issue-grouping.md](../../inception/plans/issue-grouping.md) の段階1「機能作成」16件のうち、
PO が選んだ塊を実装して `main` に入れる。**

16件の内訳（**塊の中は同じファイル・同じ判断を開く**）:

| 塊 | issue | 中身 |
|---|---|---|
| **① 遊びの芯** | #8 #37 #53 #59 #31 | 規則の当たり方と時間モデル |
| **② 手触り** | #65 #66 #67 #70 | できるのに伝わっていないところ |
| **③ 新しい仕組み** | #7 #28 #24 #40 #9 #34 #74 | 器を新しく作る |

**どこから入るかは PO 判断。**→ 質問票 [stage1-features-questions.md](../../sessions/stage1-features-questions.md)

---

## 決定済みの前提（出典あり。ここは議論しない）

| 前提 | 出典 |
|---|---|
| **方針6項目**（200日 ／ 切りのいい目標額 ／ 購入と加工が主軸 ／ 到達時に全開放でなくてよい ／ tier で配置の性格を分ける ／ 島ごとの需要差） | [what-players-should-do.md](../../inception/design-log/what-players-should-do.md) |
| **値を解かない。構造だけ決める。**仮置きの値はすべて #61 の表に1行足す（別 issue を立てない） | PO 決定 2026-09-12 ／ `aidlc-state.md`「進め方の決まり」 |
| **数値調整（段階4）は 1〜3 の後。**機能が1つ入るたびに数値の前提が変わる | PO 決定 2026-09-12 ／ `issue-grouping.md` |
| **動作確認（段階5）は最後。**#51 → #47 の順 | `issue-grouping.md`「動かせない決まり」 |
| **#73・#76 は #79 より先。**数字が嘘のまま情報を足さない | 同上 |
| **買値は全品同じ率**（`PURCHASE_RATE`）。⚠ tier ごとに率を分けない | `aidlc-state.md`「知らないと踏むこと」 |
| **規則は `scope` を宣言しない。**効き目は品の tier で按分され、店全体ぶんは**区画数で割った平均** | 同上 |
| **在庫は1つ。**`DisplaySlot` は数量を持たない | 同上 |
| **`<input>` は `src/ui/domInput.ts` を通す** ／ **通貨は `src/ui/money.ts` の1本** ／ **寸法は `src/ui/layout.ts` だけ** | 同上 |
| **「棚に手が出せるか」は `GameScene.isShelfBlocked()` の1つだけ** | 同上 |
| **文言は PO が指示する。**main は言い回しの案を先に置かない（束M で踏んだ） | PO 指摘 2026-09-12 ／ [history.md](../../history.md) |

---

## 今回決めないこと

- **数値バランス**（倍率・費用・確率・日数・上限）。仮置きしたら **#61 の表へ1行**足す
- **世界観の素材**（解像度・色・アイコン・キャラ・BGM）＝ 段階2。#10 が先頭
- **画面に足す文言の言い回し**（#79）。PO が指示する
- **テスト用機能の撤去**（#51）と**目視確認**（#47）＝ 段階5

---

## 受入条件

1. **選んだ塊の issue が `main` に入り、close されている。**
   close コメントに**コミットハッシュ**と**実装内容**を書く。本文と実装が違うなら**何をどう変えたか**も
2. **既存テスト全件パス**（着手時点 **348本**）＋ 新規機能ぶんのテストが増えている
3. `npx tsc --noEmit` と `npm run build` が通る
4. **UI に触ったら #47 の本文に1行足す**（1件ずつ確認を頼まない）
5. **「今回は扱わない」と判断したものはその場で issue にし、番号をこの文書と `issue-grouping.md` に書き戻す**
6. **仮置きの値を作ったら #61 の表に1行足す**
7. 締めるとき: 束の経緯を `aidlc-state.md` から `history.md` へ移す ／ `audit.md` を `audit-archive.md` へローテート

---

## 進め方

1. **調査**（サブ3体・並行）—— issue 本文と実装の差を洗う。⚠ **本文を信じない**（過去に6件ズレていた）
2. **PO 判断を1回にまとめて質問票へ** —— 質問ごとに中断しない
3. **塊ごとに実装** —— サブへはファイルパスを渡す（要約ではない）。目的・受入条件・触ってよい範囲・触らない範囲
4. **`main` へ入れて close** —— ブランチにあるだけでは閉じない

## 調査結果（サブ3体・2026-09-13）

⚠ **本文が実装に追い越されていた issue は、16件中 7件あった。**着手前に本文を直す必要がある。

### 塊① 遊びの芯（#8 #37 #53 #59 #31）

**5件すべてに効く、もう成り立たない前提**: 目標100万 → **1000万**（`GameService.ts:19`）／
航海日あり → **無し**（`WorldState.ts:14`）／11日周期 → **10日**（`islands.ts:33`）／
tier4・加工品70品 → **tier7・加工品85品**（実測。全135品 ＝ 50/32/30/10/7/4/2）。

⚠ **`issue-grouping.md` に書いていない上流が1つある —— #31。**
**#37・#59・#8 の3件とも、実装先が #31 の A/B で変わる。**
`#31 → #37 → #59 → #8`（#53 は独立）。⚠ **#31 を決めないと #37 の実装先が決まらない。**

| issue | 実測で分かったこと |
|---|---|
| **#37** | 本文の「素材50品」は誤り。**R5・D2 が当たるのは43品**（素材50品のうち7品は産地 `なし`）。⚠ **本文の案（産地集合）を入れると R5 の発火が 2.3% → 57.3%（25倍）。**`rules.ts:213-222` の設計目標「並べ方で客足が動く幅を最大2倍」が**常時かかる床**になる。**本文が想定していない結果** |
| **#53** | ⚠ **営業時間帯を禁じると、240分の窓で 85本中40本（47%）が着手不能・tier6/7 の6本全部が不能**（うち tier7 の2本は手際MAXでも不能）。480分なら16本。**`depth.test.ts:85-96` が確実に落ちる**（`AWAKE_MINUTES_PER_DAY = 1080` を直書き） |
| **#53** | **「深さ対広さ」は条件付きで崩れる。****升で測ると深いほど得**（利益/升 6 → 172、単調増加）／**自作の時間で測ると浅いほど得**（利益/分 2.6 → 1.3、単調減少）。⚠ **材料を市場で買えると 2.6 → 5.2 で一択に戻る。****#53 単独では方針5 を救えない**（#62・U2 と組） |
| **#59** | **待ち条件（段5 完了）はすでに満たされている。**盤面は1枚しかないので `adjacent: false` ＝ 盤面全体。⚠ `issue-grouping.md` の「**#59 は #8 の後**」に**コード上の根拠は無い**（体験上の理由）。⚠ **器の観点では逆** —— `members: ItemId[] \| Condition` が、#8 の `CustomerPreferenceRule` が従う形を決めてしまう |
| **#8** | ⚠ **#3（島ごとの需要差）は CLOSED で、「客構成」ではなく「値段倍率」で実装済み**（`islands.ts:50-55`）。**いま客構成を足すと、同じ島の差を2機構で表す**ことになる。本文が引く出典（item-system.md §2 #5）は**取り消し線の入った側**。正しくは §2 #17 |
| **#31** | 評価器に逃げているのは **3規則・4行**（`evaluate.ts:237, 254, 309, 310`）＋ **ID名指し2箇所**（`:328`）。⚠ **A 案の `ref` では R5（甲の産地 == 乙の産地）は書けない** —— `PairRule` 側の機構が別に要る。**本文はここを区別していない**。⚠ **A/B どちらでもプレイヤー体験は変わらない → 技術判断** |

### 塊③ 新しい仕組み（#7 #28 #24 #40 #9 #34 #74）

| issue | 実測で分かったこと |
|---|---|
| **#28** | ⚠ **条件2つは既存関数だけで表現できる。新しいデータは0行。**「次の島」＝ `WorldState.getLocation().next`／「その島で買えない品」＝ `stockedByIslandMerchant()`（`evaluate.ts:303`）に**次の島を差し込んで補集合**。⚠ **`routes.ts` は無関係**（あれは3ルートの金額計算で、航路の `ROUTE` とは別物）。⚠ **落とし穴**: U2（100個売ると買えるようになる）で、**発注時に買えなかった品が納品時には買える** |
| **#24** | ⚠ **本文の該当箇所が2つとも実在しない** —— `MessageWindow.ts` は無い（実体は `MessageLog.ts`）／`CharacterStrip.setShopkeeper` は無い（`drawShopkeeper` は private）。「販売ログのみ」も古く、**`event` 種別はすでに6箇所で稼働**。⚠ **#21・#15（段階2）に実質依存** —— 段階1で完結させると喋る主体が「青い丸」のまま |
| **#40** | ⚠ **「見られない」ではなく「保持していない」**（`MessageLog.ts:63-66` の `queue.shift()`、`MAX_MESSAGES = 5`）。**触るのは `MessageLog.ts` 1本だけ。7件で最小。**⚠ **#24 より先**（保持が無いまま会話を足すと、5行がさらに押し流される） |
| **#9** | ⚠ **`StockedBy` に `行商人バレン` はあるが、規則0本・評価器は読んでいない**（`evaluate.ts:317-327` は `r.id === 'U1'/'U2'` と**ID で名指し**）。**器だけ先に立っている。**⚠ **`PurchaseMenu` は島名を必須で受け取る**（産地割引・残り日数）ので、そのままでは使えない |
| **#34** | ⚠ **本文が古い**（「まず #33 を入れる」→ **#33 は束F で close 済み**／「Cycle 4 の外」→ いま段階1）。⚠ **#9 と同じ器を要求している**（島に依らない購入経路・日次の上限在庫・産地割引の逆）。**別々に作ると同じ経路が2つできる** |
| **#74** | **文言4件は実在するが行番号は全部ずれている**（束M でずれた）。⚠ **「4件消すだけ」では終わらない** —— `atSea` に依存する**分岐7箇所＋テスト1本**がある。⚠ **戻す側なら `DAYS_AT_SEA = 1` だけでほぼ済む**（導出で復活する） |
| **#7** | ⚠ **既存設計の反転になる。**`WorldState.ts:33-38` の docstring が「**現在地は日付から決まる。順序は固定で、プレイヤーは選べない**」と**自由航行の反対を明記**している。「預かり荷の区画」は**土台が1つも無い**（`FloorGrid` に塞がれた升の概念が無い）。⚠ **クリア後の話なので、200日を持たせる問題には効かない** |

⚠ **塊③をそのまま1サイクルで回すのは無理**（新規クラス4本・`SaveData` 拡張3つ・`WorldState` の設計反転）。
サブの提案は**触る場所で3つに割る**: **③-a 文字の出口**（#40 → #24）／
**③-b 島に依らない購入経路**（#9 ＋ #34 を畳む）／**③-c 航路とその先**（#74 → #28 → #7）。

### 塊② 手触り（#65 #66 #67 #70）

⚠ **`issue-grouping.md` の「触るのは `src/ui/` だけ」は2件で成り立たない**
（#66 は `evaluate.ts`、#67 は `ShelfPresets.ts` にも触る。どちらも既存の器への追加）。

| issue | 実測で分かったこと |
|---|---|
| **#65** | 読みの欄は本当に無い（`axes.ts:150-154`。`reading\|yomi\|kana` は `src/` 全体で0ヒット）。**135品中102品（75.6%）が漢字を含む。**⚠ **工房もレシピを「出来上がる品の名前」で引いている**（`CraftMenu.ts:150-154`）ので、**読みを足せば工房も同時に直る。**⚠ **`domInput.ts:87` は `input` だけを見て `compositionstart/end` を見ていない** —— 変換確定前の1打鍵ごとにヒットが動く |
| **#66** | ⚠ **本文の「仕入れ画面の行に出す」は不可能。**U2 が通らない品は `stockedByIslandMerchant()` が一覧から落とすので、**行そのものが無い。**⚠ **出せるのは持ち物一覧の数量行の 81.9px だけ**（`999個` が 32.1px。値段の行は `売123,456レン` で 79.0px を使い、残り35px に `あと100`（46.8px）は入らない）。⚠ **4行目は枠から約10px はみ出す**。**条件言語の拡張は要らない**（`累計販売数` に単調なので探索で求まる） |
| **#67** | ⚠ **本文の `546ce99` は番号を「入れた」コミット**（廃止は `a264203`）。**`savedAt` はデータとして既にある**（画面に出していないだけ）。**余白は足りている** —— 文字欄 308px に対しいま 39.4px、縦にも12pxの行が1本入る。⚠ **B 案は幅ではなく器の問題**（`refresh()` が中身をまるごと作り直す → `<input>` 10個でカーソルが飛ぶ） |
| **#70** | ⚠ **本文の「ゴーストが唯一の手がかり」は不正確。手がかりは3つある**（行の選択色 ／ `つかんでいる: …` のログ ／ 盤面のプレビュー）。⚠ **チュートリアルの引用も束M で書き換わっている。**⚠ **本文に無い挙動**: 場所にいる間の `pointerup` は早期 return して `cancelDrag` されないので、**店に戻って最初にマウスを動かすといきなりゴーストが出る**。⚠ **一覧と棚で「押して離す」の意味が逆**（棚＝補充へ飛ぶ ／ 一覧＝取り消し） |

⚠ **商人のところの行は、産地の島の品で残り 5.6px しか無い**
（`アスパラガス` ＋ `この島の産` ＋ `⚠ 切らしている（12品に要る）`）。**いまでも綱渡り。1文字も足せない。**
⚠ **工房の3ルート行も残り 9.3px**（`CRAFT_TEXT_MAX_W = 532px` に対し最悪値 522.7px）。

### 調査で見つかった、issue になっていない歪み → **issue 化した**

| 見つけたもの | 行き先 |
|---|---|
| ⚠ **`isEndlessMode` がロードで復元されない**（`GameProgress.ts:49` で保存 → `GameScene.ts:206-244` が読んでいない）。**クリア後にセーブして読み直すとエンドレスが解け、達成の幕がまた出る** ＋ **`unlockedFeatures` が死にコード** | **#80 を新設**（バグ。⚠ **#7 の前提**） |
| ⚠ **コード内の注記が古い**（`axes.ts:48`・`islands.ts:65`「77品中70品」→ **92品中85品**／`craft.ts:28`「122品」→ **135品**／`CraftMenu.ts:23,146`・`ListPaging.ts:15`「72本」→ **85本**／`ShelfPresets.ts:38` が指す `PresetMenu.test.ts` は**存在しない**） | **#81 を新設**（バグ） |
| ⚠ **目標達成の幕が「累計売上」を出している**（`GameScene.ts:1044`）／**チュートリアルの目標額が古い**（`Tutorial.ts:20` `累計売上 1,000,000レン` ←→ **所持金 10,000,000レン**）。**`GOAL_AMOUNT` が3箇所に別書き** | **#73 にコメント**（同じ型・同じ修正なので別 issue にしない） |
| ⚠ **`セーブ` / `ロード` の語がゲームのセーブと同じ**（ペルソナ2人が指摘、**どの issue にも入っていなかった**） | **#75 にコメント** |

### 本文が実装に追い越されていた issue —— **訂正をコメントで書き戻した**

**16件中 7件**: **#24**（該当箇所2つが実在しない）／**#34**（#33 は close 済み・Cycle4 の外ではない）／
**#37**（素材50品→**43品**・本文案を入れると R5 が 57.3% 発火）／**#53**（実測表が二重に無効・240分で47%着手不能）／
**#8**（出典が取り消し線側・#3 と重なる）／**#70**（引用が古い・手がかり3つ）／**#67**（`546ce99` の出典が逆）。
**#31 には main の技術判断（B）を書いた。**


## 状態

- [x] 計画ファイルを置く
- [x] 塊①〜③ の現状調査（サブ3体）
- [x] 見つけた歪みを issue 化し、番号をここと `issue-grouping.md` へ書き戻す（**#80 #81**）
- [x] 本文が古い issue 7件に訂正をコメント（#24 #34 #37 #53 #8 #70 #67）＋ #31 #73 #75
- [x] 既決・調査・技術判断を main が確定（→ 質問票の末尾）
- [x] **質問票への回答（PO・2026-09-13）** → [stage1-features-questions.md](../../sessions/stage1-features-questions.md)
- [x] 扱わないと決めたものを issue 化（**#83**）
- [x] **1波**: #74 → #28 ／ #40 → #65（`48d0851`）
- [x] **2波**: #66 → #67 → #70（`c223904`）
- [x] `main` 反映と close（**7件すべて**）／ 残作業を #85 へ切り出し
- [x] #61 に仮置きの値5件 ／ #47 に目視項目 ／ `history.md` へ経緯 ／ `audit.md` をローテート
- [ ] **次の塊は PO 判断待ち** → [stage1-next-questions.md](../../sessions/stage1-next-questions.md)

## 結果

**348 → 415 テスト。**`tsc` ／ `build` 通過。**7件 close**（#74 #28 #40 #65 #66 #67 #70）。
**この束から出た issue**: **#80 #81 #83 #84 #85。**
⚠ **#28 は器が入っただけで効いていない**（20回の寄港で目標の 0.2%）。**額は #61 ／ 構造は #85。**
⚠ **手順を1つ飛ばした** —— #28 を「残作業」を書いたまま close しかけた。**先に切り出す決まり。**#85 で直した。
