# 束F — 仕入れ・強化・クラフトを「行く場所」にする（実行計画）

**位置づけ**: [issue-grouping.md](../../inception/plans/issue-grouping.md) の束F ／ **作成**: 2026-09-12 ／ main

## 目的

**#58 を先に入れる。**いまは小さなダイアログが**棚に重なって**開く。
このゲームの判断は「**棚を見て、何が足りないか考えて、足す**」なので、
**棚を覆うことは判断の材料を隠している。**

`店 ⇄ 商人のところ ／ 工房 ／ 改装` という**場所の移動**にする。

## ⚠ #58 を束の先頭に置く理由

**束F の他5件は、すべて #58 が作る新しい画面の上に載る。**
先に作ると**作り直しになる。**残っている機能のうち、**手戻りの危険が一番大きいのがここ。**

| | 内容 | #58 との関係 |
|---|---|---|
| **#58** | 仕入れ・強化・クラフトを「行く場所」に | **これが器** |
| #33 | 次に戻るまでに要る素材の不足を見せる | どこに出すかが #58 で決まる |
| #23 | 3つの取得ルートを見せる | 同上 |
| #27 | マイセット | 同上 |
| #55 | 一覧の検索と、並べている品の見分け | 一覧が #58 で作り変わる |
| #60 | 「買えるようになった」の知らせ | 出し先が #58 で決まる |

## 決定済みの前提と出典

| 前提 | 出典 |
|---|---|
| 中央の領域が**まるごとその画面に変わる**。大きく見やすくする | #58（PO の言葉） |
| **閉じると店に帰ってくる** | 同上 |
| **左のアイテム一覧は行った先でも出してよい。**ただし**棚が見えていない間は掴んでも置けない** | 同上 |
| **行っている間は時間が進まない**（実装済み） | 同上 |
| **キャラ帯（店番・来店客）は潰さない。**#21・#15 の置き場所 | 同上 |
| ⚠ **クラフトは回数入力に HTML の `<input>` を DOM で載せている。**出し入れで付け外しが増える | 同上 |

## 今回決めないこと

- **数値バランス**（→ #61。ラベル `数値調整`）
- **画面の色と解像度**（→ 束K。#56・#10）
- **登場人物を実際に描くこと**（→ 束H・I。ここでは**置き場所を潰さない**だけ）

## 受入条件

1. `npx vitest run` 全件パス ／ `npx tsc --noEmit` ／ `npm run build`
2. **棚を覆わない**（行った先では中央が入れ替わる。重ねない）
3. **DOM の付け外しで `<input>` が残らない**（クラフトを何度も出し入れして確かめる）
4. **⚠ 満たせないもの**: 実機での目視確認 →
   **確認してほしいことを [#47] の本文に1行足す**（1件ずつ頼まない）

## 作業の順

**#58 →（画面が固まってから）#55 → #33 → #23 → #60 → #27**

- #55 は一覧そのものなので、器の直後
- #27（マイセット）は**保存の仕様を決める必要がある**ので最後
