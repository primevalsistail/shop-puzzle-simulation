# 次に何をやるか — 対象決め

**回答者**: ユーザー（PO判断）
**作成**: 2026-09-11
**状態**: 回答済み（2026-09-11）→ **A（#30）に決定**

## 何を決めるか

次のサイクルで扱う対象を1つ決める。
**優先順位はPO判断なので、こちらでは決めない。**

## なぜ今必要か

`main` は clean で、進行中のサイクルも作業中の変更も無い。
OPEN issue が 40件あるが、issue 一覧は「残っているもの」の集合であって順番ではない。
対象が決まらないと、計画ファイルの冒頭に書く目的・受入条件が書けない。

## このセッションで実測した現在地

- ブランチ `main` / `b44e40c` / 未コミットの変更なし
- `npx vitest run` → **15ファイル 162テスト 全件パス**
- `src/taxonomy/` を**本体は1箇所も import していない**（`src` 全体の grep で taxonomy 外 0件）。
  ゲーム本体は今も `src/data/items.ts`（旧13品）で動いている
- **島は本体に存在しない。**「島」に触れるファイルは `src/taxonomy/` の7本だけ
- `TimeManager` の初期値は `{ day: 1, hour: 8 }`。`WAKE_HOUR = 6` は**睡眠明けの時刻**で、
  開始時刻とは別物（#41 は未実施）
- **⚠ 環境の制約**: ブラウザ実機確認ができない（Chromium が exit 144）。
  UI に触る作業は、テスト・型・ビルドまでしか保証できない

## 今回決めないこと

- 選ばなかった対象を閉じるかどうか（issue は開いたまま残す）
- 実機確認の分担（対象が決まってから、その計画ファイルで決める）

---

## Question 1

次にどれを進めますか。

**A) #30 — `src/taxonomy` をゲーム本体に接続する**
- 内容: 旧 `src/data/items.ts`（13品）から `src/taxonomy`（122品・レシピ72本・積み上げ価格・条件規則）へ移す
- 効く範囲: これが入ると **#19（取り合わせを条件規則へ）と #22（利幅バランス）が閉じられる**。
  今は「`src/taxonomy` 側では出来ているが本体が旧データで動いている」ため閉じられない
- 規模: 大きい。本体の ItemRegistry・Inventory・CraftingSystem・売買の各所に波及する
- 出典: [item-taxonomy-integration-plan.md](../../construction/plans/item-taxonomy-integration-plan.md)（Q1 が未回答）

**B) #45 — 数値バランスの前提を測り直し、#35・#36 の要否を判定する**
- 内容: 3層価格構造と時間モデルが入った後の値で測り直す。**#35・#36 の本文の数字は古い前提のまま**
- 効く範囲: 測った結果次第で #35・#36 は「直す」にも「閉じる」にもなる
- 規模: 中。まず計測だけで切れる（直すかは計測後の別判断）
- 注意: 計測対象は `src/taxonomy` 側の値で、**本体の体験には A を通すまで出ない**

**C) 小さい不具合・操作性をまとめて片づける（#41・#43・#44）**
- 内容: #41 開始時刻を6時にする ／ #43 1日を終わらせるショートカット（テスト用） ／ #44 島・日付の表示
- 効く範囲: 以後の手動確認が速くなる
- 規模: 小。ただし **#44 は島が本体に無いので、日付の表示までしか出来ない**
  （島の表示には #2 島の巡回システムが要る）

**D) Cycle 5 — 客と需要を新サイクルとして起票する**
- 内容: 客の来訪モデル（#8 ペルソナと購買傾向・#11 客が来る強化要素）を設計から始める
- 効く範囲: 売上の出方そのもの。**現状はコードにも設計にも存在しない**
- 規模: 大きい。Inception（Requirements → Application Design）から始まる

**X) Other**（[Answer]: の後に記述してください）

**こちらからの推奨: A。**
理由は好みではなく依存関係で、**B・C・D のどれを先にやっても、成果がプレイヤーに届くのは A を通した後になる**から。
B の計測値も、C の #44 の島表示も、D の需要も、本体が旧13品のままでは画面に出ない。
**ただしこれは「どれが先に効くか」の話であって、何を優先するかはPO判断。**
A を後回しにする理由（大きすぎる・先に手触りを直したい等）があれば B〜D を選んでよい。

[Answer]: 優先度の高いものをこなしたい。推奨ないしはissueの中で優先して解決するべき事象を対応したい

**→ 決定: A（#30 本体接続）。**「推奨ないしは」を推奨への委任と読み、A とした。
続きは [item-taxonomy-integration-plan.md](../../construction/plans/item-taxonomy-integration-plan.md)。

