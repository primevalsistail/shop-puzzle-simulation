# 次のサイクルで何をやるか

**作成**: 2026-09-13 ／ **回答者**: PO
**前提**: 作業ツリーはクリーン（`a8c4b2b`）／512テスト全件パス／OPEN 30件

---

## 答えてほしいこと（1つ）

## Question 1
次のサイクルで着手する塊はどれですか。

A) **#87 魔法の品と武器を作る**（**推奨**）
   PO 判断が全部出ていて、依存する issue も無い。着手前の調査なしにそのまま実装に入れる唯一の塊。
   触るのは `src/taxonomy/items.ts` ／ `recipes.ts` と、その不変条件テスト。
   ⚠ 品数が増えるので #61（数値の一括測り直し）の前提が動く。これは想定内（#86 で 135 → 145品にしたのと同じ）。

B) **#8 ／ #37 ／ #59 —— 規則と客**
   3件とも**着手前に判断が1つずつ要る**。
   #8 は「島の差を客構成で表すか、いまの値段倍率（#39）で表すか」。
   #37 は「R5 の条件を絞り直すか」（本文の案のままだと発火が 2.3% → 57.3% になり、設計目標が壊れる）。
   #59 は「同棚セットのデータを入れるかどうか」。
   → 選ぶ場合、実装の前に質問票をもう1枚出す。

C) **#7 買い取り達成＝エンディングと自由航行**
   `WorldState` の設計（「現在地は日付から決まる。プレイヤーは選べない」）を**反転させる**。
   ⚠ 島順が固定だから「次はノアキータだから今のうちに買う」という先読みが成立している。
   選べるようにすると、方針6（島ごとの需要で往復させる）の強制力が消える。
   → 選ぶ場合、まず「何を選べるようにするか」の設計をやる。実装は同じサイクルに入らない可能性がある。

D) **段階2（世界観）の続き —— #10 解像度 → #56 色 → …**
   ⚠ **別セッションが同じ段階を進めている**（主人公の三面図 ／ #82 図鑑）。
   このセッションでも触ると、同じリポジトリで衝突する。
   → 選ぶ場合、**向こうのセッションを閉じてから**にしたい。

X) その他（[Answer]: の後に書いてください）

[Answer]: A

---

## 答えなくて構いません（報告）

### いまの状態

- **作業ツリーはクリーン。**`main` は `a8c4b2b`（「次のセッションへ引き継ぐ」）
- **512テスト全件パス** ／ `tsc --noEmit` ／ `npm run build` 成功
- **OPEN 30件。**段階3（バグ）は 2026-09-13 に空になった

### 段階4・5 は動かせない

- **段階4（数値調整・11件）は 1〜3 の後。**機能が1つ入るたびに数値の前提が変わる
- **段階5（動作確認・2件）は最後。**#51（確認用の表示を外す）→ #47（目視）の順

### 着手が決まったら main がやること

1. 対象 issue の**本文と現状の差を洗う**（2026-09-13 の調査では 16件中7件で本文が実装に追い越されていた）
2. `construction/plans/` に計画ファイルを作り、**目的・決定済みの前提と出典・今回決めないこと・受入条件**を先に書く
3. その上で実装に入る（サブへ委任する場合も同じ）

### 持ち越しの残り

⚠ **`audit.md`（65KB）のローテートは前回見送った** —— 別セッションの記録と交互に入っていて、
向こうがまだ書き足している最中だったため。**次の区切りで、向こうが落ち着いてから。**
