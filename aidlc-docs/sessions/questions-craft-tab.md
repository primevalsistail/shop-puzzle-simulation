# 工房の赤入れ（2026-09-13）—— 3つ答えてください

**作成**: 2026-09-13 ／ **回答者**: PO

**表そのものは入れました**（商品 ／ 作成数 ／ 在庫 ／ 時間 ／ 需要 ／ 数量 ／ 作る）。
**1件1行になり、1ページ 5行 → 11行**（ページ 17 → 8）になっています。
残ったのは、**図に列が無かった2つの行き先**と、**`需要` という列名**です。

---

## 答えてほしいこと（3つ）

## Question 1
**`需要` の列に、産地と「売れる島」のどちらを出しますか。**

**いまどうなっているか**
**図の `ノアキータ` をそのまま出しています**（＝**材料の産地**）。
これは 3段組みのときに `蕎麦の実×3(400)＠ノアキータ` の `＠` で付いていたもので、
**「その材料は、いまの島では買えない。ノアキータへ行くと買える」**という意味です。

⚠ **「売れる島」と読むと、図の行は空になります。**
**実測**: 出来上がる `蕎麦粉` は `向く土地: どこでも` なので、**需要で高く売れる島がありません。**
**つまり図に書かれた `ノアキータ` は、売れる島ではありえません。**

**選べるもの**

A) **中身は産地のまま、列の名を `産地` に変える**（**推奨**）
   **図の値と、列の名が合います。**⚠ **いちばん小さい直しです。**

B) **中身は産地のまま、列の名も `需要` のまま**
   ⚠ **名前と中身が違います。**「ノアキータで売れる」と読み違えます。

C) **中身を「売れる島」に変える**（列の名は `需要` のまま）
   **画面に無かった情報が増えます。**
   ⚠ **106レシピ中 42 は `どこでも` なので、その行は空になります。**
   ⚠ **産地（＝どこで材料を買うか）は画面から消えます**（→ Q2 と同じ話になります）。

D) **列を2つにする**（`産地` と `需要`）
   ⚠ **横幅がもうありません。**他の列を削ることになります。

X) その他（[Answer]: の後に書いてください）

[Answer]:

---

## Question 2
**材料の一覧（`蕎麦の実×3(400)`）を、どこで見せますか。**（→ #107）

**いまどうなっているか**
**図に列が無いので、行から落ちました。**
作れないときは `作る` ボタンの字が `材料不足` に変わりますが、
⚠ **どの材料が何個足りないのかは、工房の画面から分かりません。**
（材料の在庫は、左の持ち物一覧か仕入れの画面で見ることになります）

**選べるもの**

A) **このまま出さない**（**推奨**）
   ⚠ **いちばん小さい。**図のとおりです。
   **足りないかどうかは `作る` ボタンが答えている**ので、
   **何を買えばいいかは `需要`（産地）の列が指しています。**

B) **`材料` の列を足す**
   ⚠ **横幅がもうありません。**`時間` か `需要` を削ることになります。

C) **行を押したら出す**（別の窓・折りたたみ）
   ⚠ **押さないと見えません。**作りも増えます。

X) その他

[Answer]:

---

## Question 3
**儲け方の3ルート（`転売+12レン → 作る+129レン`）を、どこで見せますか。**（→ #108）

**いまどうなっているか**
**図に列が無いので、行から落ちました。**

⚠ **これは #23 で「見えていないから」と言って足したものです。**
**画面から消すと、#23 の前に戻ります。**
（計算そのもの（`routeValues`）は残してあるので、**出し先さえ決まれば戻せます**）

**選べるもの**

A) **図鑑（#82）で見せる**（**推奨**）
   **「どう儲けるか」は品ごとに1回読めば足りる**ので、**毎行に出す情報ではありません。**
   ⚠ **#82 はまだ着手していません。**その間は画面のどこにも出ません。

B) **このまま出さない**
   ⚠ **#23 が実質無くなります。**

C) **列を足して工房に残す**
   ⚠ **横幅がもうありません。**3つ並べて初めて比べられる行なので、**削ると意味が消えます。**

X) その他

[Answer]:

---

## 答えなくて構いません（報告）

- **行が 84px → 40px になり、1ページ 11行**入るようになりました（ページ 17 → 8）
- **`回` の字は無くなりました**（図に無いため）。**`最大` は `数量` の列に入りました**
- **`-10` `+10` を足しました。**⚠ **1より下にはなりません**（`-10` を押しても 1 で止まります）
- **作れない理由は `作る` ボタンの字**になりました ——
  `材料不足` ／ `在庫上限` ／ `時間切れ` ／ `回数を入れて` ／ `1以上の整数`。
  ⚠ **`在庫が上限 400` の `400` は落ちました**（ボタンに入らないため）
- **`時間` の列に `（営業◯分）` は今までどおり出ます**（#53）
- **見出しの字は一覧の上に1回だけ**です（納品タブと同じ）。
  ⚠ **改装タブの同じ問い（`questions-upgrade-tab.md` Q2）は、これで先に決まった形になります**
- **ヘッドレスの Chromium で開いて確認しました。JS エラー0件**
