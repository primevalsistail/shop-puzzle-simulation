# AI-DLC State

## Project Information
- **Project Name**: 在庫配置パズル型 店舗経営シミュレーション
- **Project Type**: Brownfield（既存コードあり）
- **Tech Stack**: TypeScript + Phaser.js 3 + Vite
- **Platform**: Webブラウザ
- **Started**: 2026-07-05T02:00:00Z
- **Last Updated**: 2026-09-13（**段階1「機能作成」の着手前調査を完了。質問票への回答待ち**）

## Current Status

### ⚠ 次のセッションはここから

1. **この節と「いまの作りで、知らないと踏むこと」を読む。**それ以外は必要になってから
2. **どの段階から入るかを PO に聞く**（下の5段階。**動かせない決まりは3つだけ**）
3. **着手前に、対象 issue の本文と現状の差を洗う。**
   ⚠ **本文が実装に追い越されている issue が実際にあった**（束F で3件・2026-09-12 に2件）
4. **計画ファイルを `construction/plans/` に作り、目的・決定済みの前提・今回決めないこと・
   受入条件を先に書く。**サブへ委任する場合も自分でやる場合も省かない


**束M（画面に出す言葉）は締めた**（2026-09-12。`f93ca59` ／ `d17392c` ／ `14646ed` ／ `33a4bda`。
**#68・#1 を close**）。**348テスト ／ `tsc` ／ `build` ／ 実機確認。**
⚠ **束の切り方を変えた**（PO 判断 2026-09-12）。**9つの束は細かすぎた。**
→ **機能作成 → 世界観 → バグ → 数値調整 → 動作確認**の5段階。
**OPEN 42件すべてがどれかに入っている**（16+10+3+11+2）。
→ [issue-grouping.md](inception/plans/issue-grouping.md)

⚠ **動かせないのは3つだけ**: **数値調整は1〜3の後** ／ **動作確認は最後**（#51 → #47）／
**#73・#76 は #79 より先**（数字が嘘のまま情報を足しても、足した情報も嘘になる）。
**それ以外は PO が入れ替えてよい。**

**段階1「機能作成」の着手前調査は完了した**（2026-09-13。サブ3体）。
⚠ **16件中 7件で、issue 本文が実装に追い越されていた。**すべて issue のコメントに訂正を書き戻した。
**新設**: **#80**（クリア後の状態がロードで復元されない。⚠ **#7 の前提**）／**#81**（コード内の注記が古い）。
**main が確定させた技術判断**: **#31 = B（足さない）** ／ **#34 は #9 に畳む**。

⚠ **PO の回答待ち** → [stage1-features-questions.md](sessions/stage1-features-questions.md)
**調査の全文** → [stage1-features-plan.md](construction/plans/stage1-features-plan.md)

**段階2「世界観」の #15（キャラグラフィック）に先行して着手した**（PO 指示 2026-09-13）。
⚠ **絵に関する決定はゼロだった** —— 色の指定が一色も無く、**画像アセットも1枚も無い**
（`BootScene.ts` の `load.*` が0件）。人物像のほうは8人とも確定済み。
**三面図を基準にし、SD はその後**という段取り。
⚠ **PO の回答待ち（3問）** → [character-visual-questions.md](sessions/character-visual-questions.md)
**土台** → [char-visual.md](inception/worldbuilding/char-visual.md)
／ **計画** → [character-visual-plan.md](construction/plans/character-visual-plan.md)

### 直近で入ったもの

**束M（画面に出す言葉）**: `¥` → **`レン`** ／ 画面ごとの判断に要らない文字の削除 ／
**呼び名の統一**（ボタンと行った先の見出しが3つとも同じ語）。
⚠ **足す側は1件も入っていない → #79**（**文言は PO が指示する**）。
**この束から出た issue**: #73 #74 #75 #76 #79。
→ 詳細は [history.md](history.md) ／ [bundle-m-text-decisions.md](construction/plans/bundle-m-text-decisions.md)
／ [persona-review-words.md](inception/design-log/persona-review-words.md)

**方針6項目の骨格はひととおり揃った。**
**段取り → [issue-grouping.md](inception/plans/issue-grouping.md)（2026-09-12 に5段階へ切り直した）**
**数値調整は最後**（ラベル `数値調整` ／ #61）。

⚠ **`数値調整` の判定基準**: 「**新しい機能を足すか、既にあるものの値と割り当てを直すか**」。
後者はすべて #61 の後ろ。→ [policy-rework-plan.md](construction/plans/policy-rework-plan.md)

### まず読むもの（一次情報）

1. **[what-players-should-do.md](inception/design-log/what-players-should-do.md)** —
   PO が指定した方針6項目。**議論の対象ではない**
2. **[what-this-game-is.md](inception/design-log/what-this-game-is.md)** — ゲームの構造

### リポジトリの状態（2026-09-12）

**348テスト全件パス** ／ `tsc --noEmit` ／ `npm run build` 成功。**作業ツリーはきれい。**
OPEN issue **44件**（段取りは [issue-grouping.md](inception/plans/issue-grouping.md)）。
**直近のコミット**: `4e72d62`（段取り）／ `33a4bda`（工房の帯）／ `f93ca59`（束M 本体）。

**ブラウザで実際に動かして確認済み**（2026-09-12）。⚠ **やり方はこの資料に残す価値がある**:
プロジェクトに起動用スキルは無く `chromium-cli`／`playwright` も入っていないが、
**`~/.cache/ms-playwright/` に Chromium のバイナリがある。**
`playwright-core` を **`/tmp/pwdriver`（プロジェクトの外）**に入れれば動かせる。
⚠ **`package.json` を触らないこと。**
**画面は 1280×720 固定**なので、viewport を同じにすれば**ゲーム座標＝ページ座標**になる。

### 進捗

| 段 | 中身 | 状態 |
|---|---|---|
| 段1 | 買値の統一・航海日なし・クリア判定・集客上限・巡回順・目標1000万 | **完了** |
| 段2 | 強化4系統×5段・手際の配線・速度切り替え・節目で止まる | **完了** |
| 段3 | **ボーナス構造**（tier で「薄い全体／濃い個別」を割り当てる） | **完了** |
| 段4 | 島ごとの需要 ＋ 滞在中の解禁 | **完了**（残りは #63・#64） |
| 段5 | tier7 まで拡張（品・レシピ追加。高tierほど大きい升） | **完了** |
| ~~段6~~ | **数値の測り直しは段ではない。**ラベル `数値調整` の束（#61）＝ **今あるすべての issue の後** | |

**方針6項目は、6つとも骨格が入った。**残っているのは
**「200日の後半をどう持たせるか」**（解禁だけでは埋まらない）と、個々の未実装機能。

### いまの作りで、知らないと踏むこと

- **在庫は1つ。**`DisplaySlot` は数量を持たない。**店＝船倉**で倉と売り場は同じ場所なので、
  区画は「どの品を、どこに、どの向きで出しているか」だけを表す。
  **「補充」は「もっと手に入れる」の意味**で、棚をクリックすると仕入れ／クラフトへ飛ぶ
- **仕入れ・クラフト・改装は「行く場所」**（#58）。中央の領域がまるごと入れ替わり、棚は**消える**。
  ⚠ **覆うのではなく消す**（盤面は最大 x982 まで伸び、場所の領域 976 を6px はみ出す）。
  寸法は **`src/ui/layout.ts` だけ**が持つ。⚠ **写しを作らないこと**
- ⚠ **「棚に手が出せるか」の判定は `GameScene.isShelfBlocked()` の1つだけ。**
  以前は同じ `||` の連鎖が7箇所に散らばり、**1箇所だけ強化メニューが抜けていた**
- ⚠ **`<input>` を一覧と同じ入れ物に入れない。**打鍵のたびに作り直されてカーソルが飛ぶ。
  `SearchBox` と `CraftMenu` の回数入力はどちらも**開くとき1度だけ**置いている
- **買値は全品同じ率**（`PURCHASE_RATE`）。これだけで
  `作る利益 − 転売利益 ＝ 加工利益 × 買値率` が成り立ち、
  **転売は薄利／作るほうが儲かる／深く作るほど取り分が増える**が同時に満たされる。
  ⚠ **tier ごとに率を分けないこと。**分けると符号が揃わなくなる
- **tier は「加工の深さ」であって「高級さ」ではない。**高級さは `贅沢さ` の軸
- **規則は適用範囲（`scope`）を宣言しない。**発火した効き目は**その品の tier で按分**され、
  一部が店全体へ、残りがその区画へ行く（tier1 は 90% が店全体／tier7 は 90% がその区画）。
  ⚠ **店全体ぶんは「置いてある区画数で割った平均」。**合計にすると効果の総量が区画数の
  **2乗**で伸びる（実測 13×10 で値段65倍）。**上限を足して抑えようとしないこと**
- **未調整の値**: 強化費（総額 2,236,000レン ＝ 目標の22%）／**R5 の集客 1.6**。**#61 で測り直す**
- ⚠ **`materials.ts` の素材展開は `outputQuantity` で割る。**85本中59本が2個以上なので、
  割らないと要る素材を**段ごとに最大4倍**に見積もる（#33 で一度やって #23 で直した）
- ⚠ **破棄ゾーン（外周56px）から盤面をくり抜いている**（#72）。
  上端 y0〜56 と盤面の1行目 y8〜66 が重なるので、くり抜かないと
  **一番上の行へ動かすと棚から外れる。判定と描画の両方でくり抜くこと**（`rects.ts`）
- ⚠ **0件のとき「理由」を書き分ける。**「絞り込んだ結果」と「そもそも無い」は別物で、
  一緒にすると**序盤に何も知らないまま『揃っている』と言われる**（#48 の教訓。
  `CraftMenu` は避けていたが `PurchaseMenu` に移っておらず、実機確認で出た）
- ⚠ **`<input>` は `src/ui/domInput.ts` を通す**（工房の回数／一覧の検索／仕入れの個数）。
  打鍵をゲームへ漏らさない・DOM が無くても落ちない・消すときキー入力を戻す・ESC の逃げ道 ——
  **1箇所でも忘れると操作が効かなくなる**
- ⚠ **通貨の組み立ては `src/ui/money.ts` の1本だけ**（`1,234レン`）。
  **`¥` の頃は16箇所に手書きで、桁区切りの有無すら揃っていなかった。**
  ⚠ **`レン` は全角2文字で `¥` より広い。****金額を出す場所を増やすときは幅を測ること**
- ⚠ **「枠に収まるか」は `layout.test.ts` が `estTextWidth` で見ている。**
  字種ごとの係数は**実測値**（数字 0.64 ／ 区切りと空白 0.34 ／ それ以外の半角 0.84 ／ 全角 1.0）。
  ⚠ **見積もりが実測を下回ったらテストが嘘をつく。**その検査自体も入っている
- ⚠ **見出しの下の横線は `layout.ts` の `TITLE_RULE_Y`。**
  `PlaceFrame` が `TITLE_Y + 20` と直書きしていて、**帯を動かす側から見えず、一度壊した**
- ⚠ **知らせの重複抑止は `MessageLog` の側にある**（`info` の同じ文を続けて出さない）。
  **呼び出し側に印を持たせないこと。**同じ失敗が別の経路でまた出る。
  ⚠ **`sale` と `event` は抑止しない**（出来事は回数が情報）
- ⚠ **段3 で1日の売上が 30% に落ちた**（13×10 で ¥248,743 → ¥74,889）。
  段3 前は `0.15 × 集客8.0 = 1.2` で**確率が1を超え、毎分かならず客が来ていた**。
  **目標1000万・200日はこの上で測り直す**

### 進め方の決まり

- **⚠ 値を解かない。構造だけ決める**（PO 決定 2026-09-12）。
  **後回しにできるのは値**（倍率・費用・確率・日数・上限）、
  **できないのは構造**（どの効き目を使うか／どこに差を置くか／何を上限にするか）。
  構造を後から変えると、その上で測った値が全部無効になる。
  **仮置きの値はすべて #61 に集める**（ラベル `数値調整`）。
  ⚠ **測り直すのは「今あるすべての issue を片付けたあと」**（PO 決定）。段5 の次ではない。
  **機能が1つ入るたびに数値の前提が変わる**ので、先に測ってもその測定は次の機能で無効になる。
  ⚠ **新しく仮置きの値を作ったら #61 の表に1行足す。**別 issue を立てない。
  例外は**符号が反転する変更**（例: 産地割引で「作る > 転売」が崩れないか）。これは構造の確認

- **UI に触ったら、確認してほしいことを #47 の本文に1行足す。**
  テスト・型・ビルドが通っていても、画面の不具合はそこでしか出ない。
  ⚠ **確認は1件ずつ頼まない。**#47 にためて**最後にまとめて**見てもらう（PO 決定）。
  `npm run dev -- --port 5173 --strictPort` は動き、**ユーザーは見られる**
- ⚠ **先に言葉を置くと、判断がその言葉に寄る。**
  **消す／直す理由を書くときは、それが本当にその箇所に当たっているかを確かめる。**
  束M で `まだ作れるものが無い` を誤った理由で消し、**ペルソナ4人全員に指摘されて戻した**（→ [history.md](history.md)）。
  **だから #79 には言い回しの案を書いていない。**
- **サブへ委任するときは、前提を1つ残らず依頼書に書く。**
  先例: [economy-rebalance-handoff-2.md](construction/plans/economy-rebalance-handoff-2.md)
- **確認用の道具**（`src/debug/`）: `M` 金+100万 ／ `N` 1日 ／ `B` 10日 ／
  `I` この島の品を各100 ／ `O` 全品を各100 ／ `U` 強化を全系統1段。
  **出荷前に丸ごと外す**（#51）。外すのは `src/debug/` と `GameScene` の呼び出し1箇所

### ペルソナが挙げた危うい点

→ [persona-review.md](inception/design-log/persona-review.md)

4人全員が評価したのは**島ごとの需要差**。危ういのは
**①拡張がパズルを殺す ②資源が実質お金だけ ③200日の中身が示されていない。**
**①は段5（高tierほど大きい升）で、③は段3〜5で答えることになる。**

⚠ **束M で2巡目のレビューもした** → [persona-review-words.md](inception/design-log/persona-review-words.md)。
**初日に開く4画面のうち3つで判断が成立していない**と言われ、**4件を直した。**
**残りは #79。**⚠ **文言は PO が指示する。**

---

## これまでの経緯

**過去のサイクルの記録は [history.md](history.md) に移した。**
**束F より前のやり取りは [audit-archive.md](audit-archive.md) に移した**（2026-09-12）。

⚠ **経緯を現在地として読まないこと。**あちらには、いまはもう成り立たない数字が残っている
（航海日があった頃の周期、買値率、目標額、それらの上で測った日数など）。
**いま何がどうなっているかは、この文書の Current Status だけを見ること。**
