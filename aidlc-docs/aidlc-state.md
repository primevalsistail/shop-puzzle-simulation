# AI-DLC State

- **Project**: 在庫配置パズル型 店舗経営シミュレーション
- **Type**: Brownfield ／ TypeScript + Phaser.js 3 + Vite ／ Web ブラウザ
- **Started**: 2026-07-05 ／ **Last Updated**: 2026-09-14（**`main` が最新。作業ブランチの中身はすべて入り済み**）

---

## ⚠ 次のセッションはここから

1. **この節と「知らないと踏むこと」を読む。**それ以外は必要になってから
2. ⚠ **`main` は最新。作業ブランチは無い。**
   **次に何をやるかは PO 待ち** → [questions-stage4-continue.md](sessions/questions-stage4-continue.md)
3. ⚠ **着手前に、対象 issue の本文と現状の差を洗う。**
   **2026-09-13 の調査では 16件中7件で、本文が実装に追い越されていた**
4. **計画ファイルを `construction/plans/` に作り、目的・決定済みの前提・今回決めないこと・
   受入条件を先に書く。**サブへ委任する場合も自分でやる場合も省かない
5. ⚠ **issue を勝手に増やさないこと**（PO 指示 2026-09-15）。
   **スコープ外を見つけたら、まず「このサイクルで直せるか」を見る。**
   **直せないものは計画ファイルに書いて報告し、起票は PO に確認してから**
6. ⚠ **PO とのやり取りに、文書の中だけの語を持ち出さないこと**（PO 指示 2026-09-15）。
   **「幕」「見出し下」で2つ同時に止められた。画面の話は見たままの言葉で書く。**
   ⚠ **PO が見ていない文言について判断を求めない。**
   **画面で見せるか、こちらで決めて報告するかの二択**

**直近で入ったもの**（2026-09-14。**経緯は [history.md](history.md) と `audit.md`**）:
**数値調整（段階4）に入った** —— **測る道具 `src/sim/` ができた**（本番を import して N 日回す）／
**加工時間から tier の二重掛けを外した**（tier4 の初期 14時間 → 10〜29分）／
**値段の決まり方を作り直した**（**加工利益 ＝ 係数 × 升数^k × 格の倍率**。時間も tier も出力数も入らない）／
**目標額 1,000万 → 200万**（商船の値段も同じ）／
**救済の品を置き、`money <= 0` の GAME OVER を外した**／
**画面の色を `ui/palette.ts` に集約**（生の16進 232箇所・155色）。

⚠ **`node_modules` はセッションを跨ぐと消える。**`npm install` から始める（`package.json` は触らない）。

### そのほかに残っているもの（2026-09-14 時点・OPEN 25件）

| | 中身 | 状態 |
|---|---|---|
| **数値調整（11件）** | #35 #38 #39 #45 #52 #54 #61 #62 #63 #71 #112 | ⚠ **いま走っている束。道具で測れば要否が一度に決まる** |
| **世界観（10件）** | #6 #14 #15 #20 #21 #69 #75 #79 #82 #114 | ⚠ **別セッションが進めている。同じファイルを二重に触らないこと** |
| **画面の赤入れの残り（2件）** | #109（加工が営業を削る知らせ）／ #113（確認の ON/OFF） | ⚠ **#109 は数値調整の結果で前提が変わる** |
| **出荷前（2件）** | #51（テスト用の機能の撤去）→ #47（目視） | ⚠ **いちばん最後** |

⚠ **道具を回して見えた引っかかりが5つ。うち3つが未着手** ——
**① 深く作るほうが遅い**（設計の不変条件と逆）／
**③ 棚の升が余る**（130升に広げても 18〜33区画しか埋まらない。**1品は棚に1区画まで**）／
**⑤ 客ロス分が全方針で常に0**（**加工が営業を削るという設計が、いまの数では一度も効いていない**）。
**② tier4 に届かない**は原因が割れた（**材料の島跨ぎ**。tier4 の20本中18本が 2〜4島）。
**④ 納品が取り分の 0.3〜0.6%** は #54 と一緒に見る。
**経緯は [stage4-start.md](construction/plans/stage4-start.md)。**

⚠ **答えの出ていない質問票が4枚あるが、中身が古い** ——
**答えを待たずに実装が進み、issue（#102 #104 #105 #106 #107 #108）が先に close された。**
**扱いは [questions-stage4-continue.md](sessions/questions-stage4-continue.md) Q2 で PO に聞いている。**

### リポジトリの状態

**`main` = `8819f86`。origin と同じ。762テスト全件パス。**
⚠ **ローカルの `feat/stage4-start` と `feat/rescue-nogameover` は入り済みで、消せる。**
**OPEN 25件**（段取りは [issue-grouping.md](inception/plans/issue-grouping.md)。⚠ **その文書の件数 35 は古い**）。
⚠ **「機能作成」と「バグ」は空になった。**

⚠ **次に何をやるかは PO 待ち** → [questions-stage4-continue.md](sessions/questions-stage4-continue.md)。

⚠ **ブラウザで動かす方法**（プロジェクトに起動用スキルは無い）:
`chromium-cli`／`playwright` は入っていないが、**`~/.cache/ms-playwright/` に Chromium のバイナリがある。**
`playwright-core` を **`/tmp/pwdriver`（プロジェクトの外）**に入れれば動かせる。
⚠ **`package.json` を触らないこと。**
**画面は 1920×1080 固定**なので、viewport を同じにすれば**ゲーム座標＝ページ座標**になる。
`npm run dev -- --port 5173 --strictPort` で起動する。

⚠ **数を測る道具**: `npx vite-node src/sim/run.ts -- --days=400 --seed=1 --policy=shallow`。
**方針は「全部転売」「浅い品だけ作る」「深い品を作る」「取り合わせを狙う」の4つ。**
**400日で 50〜130秒／方針。`npx vitest run` からは走らない。**

### まず読むもの（一次情報）

1. **[what-players-should-do.md](inception/design-log/what-players-should-do.md)** —
   PO が指定した方針6項目。**議論の対象ではない**
2. **[what-this-game-is.md](inception/design-log/what-this-game-is.md)** — ゲームの構造
3. **[island-goods.md §0](inception/worldbuilding/island-goods.md)** — **品を作るときに守る線**
4. **[item-system.md §3](inception/design-log/item-system.md)** — **却下した分類案と理由**

---

## いまの作りで、知らないと踏むこと

- ⚠ **内部座標は 1920×1080**（#10 ／ 2026-09-15。1280×720 から**すべて正確に ×1.5**した）。
  ⚠ **`pixelArt: true` を戻さないこと。**いまは `antialias: true` ＋ `roundPixels: false`。
  **`image-rendering: pixelated` は縮小時に間引きになり、内部座標を上げるとかえって汚くなる。**
  ⚠ **`layout.ts` のコメントの px は一部が 1280系のまま**（→ #117 で A だけ直した）。
  **残っているのは Chromium での実測値で、掛けた数は誰も測っていない。冒頭に断りがある**
- ⚠ **画面に出す文字の大きさは `layout.ts` の `*_FONT_PX` が全部持っている**（**80個**。#116）。
  ⚠ **`fontSize: 'Npx'` を画面側に直接書かないこと。**`layout.test.ts` から見えなくなる。
  ⚠ **値が同じでも定数を使い回さないこと** —— **片方を動かすともう片方が黙って動く**
- ⚠ **目標と GAME OVER の判定は `GameService.checkGoalAndGameOver()` の1箇所**（#93）。
  **`TIME_MINUTE_PASSED` の購読で `onMinutePassed()` の*直後*に呼ぶ。**
  - **外**なので `isOpen` にも棚の空にも遮られない（**棚が空でも閉店中でも見る**）
  - **後ろ**であることが要点。⚠ **`ECONOMY_MONEY_CHANGED` へ移さないこと** ——
    **`canAfford` は `>=` なので残金ちょうどの仕入れが通り、その場で GAME OVER になる。**
    **「所持金を全部仕入れに突っ込む」は正当な戦略**で、即死にするのは退行
  - ⚠ **加工で飛ばしたあいだ（`TIME_SKIPPED`）は判定しない**（#25 と同じ扱い）
- ⚠ **エンディングの入口は「商船を買う」だけ**（#97 ／ 2026-09-15）。
  **目標額に届いても幕は出ない**（`PROGRESS_GOAL_COMPLETE` は誰も出さない）。
  ⚠ **`isEndlessMode` の意味は「商船を買った＝エンディングを見た」。**
  **`SaveData` のフィールド名は変えていない**（変えると今あるセーブが読めない）。
  ⚠ **商船の値段＝目標額なので、ぴったりで買うと所持金が 0 になる。**
  **`buyShip()` は旗を先に立ててから代金を引く。**⚠ **裏返すと買ったあとは GAME OVER が出ない** → **#121**
- ⚠ **全画面の知らせ（エンディング・GAME OVER）を閉じるときは `curtainShown` を戻すこと**（#118）。
  **`isOverlayOpen()` が読んでいるので、戻さないと以降 `<input>` が全部隠れたままになる。**
  ⚠ **ロードでも戻す。**⚠ **`showGameOver()` のほうは戻さない**（消す口が無い終わりの知らせ）
- ⚠ **改装タブだけ一覧の上端が違う**（`UPGRADE_HEAD_Y` 120 ／ `UPGRADE_ROWS_TOP` 150。
  ほかの2タブは `ROWS_TOP` 165）。**改装は絞り込みの帯もページ送りの帯も使っていないので、
  そのぶんを一覧に回して5行入れた**（PO 赤入れ 2026-09-15）。
  ⚠ **タブを行き来すると一覧が 15px 動く。**これは承知のうえの引き換え
- ⚠ **タブの中で「買ったら閉じる」ものは、閉じたあとに建て直さないこと。**
  **`UpgradeMenu.buyShip()` が `onShipBought()`（中で取引が閉じる）のあとに `rebuild()` を呼び、
  棚が戻っているのに改装の行が描かれたままになった**（2026-09-15 にブラウザで踏んだ）。
  **`if (this.isOpen)` を見てから建て直す**
- ⚠ **レシピの解禁は「材料が全種そろったら、そのレシピだけが即開く」**（#111 ／ 2026-09-14）。
  **日付で増える枠は消した**（`allowedGroupCount` ／ `UNLOCK_INTERVAL_DAYS` ／ 系統は無い）。
  ⚠ **「1寄港あたり2〜3回」という PO 指定はここで無効になった。**
  ⚠ **「材料のどれか1つ」にしないこと** —— **実測で Day 1 に 47/106本、うち tier3以上が24本開く**
  （全種なら5本）。**塩1品で12本開く**
- ⚠ **確認ダイアログは `src/ui/ConfirmDialog.ts` の1つだけ**（2026-09-14）。
  **全画面の暗幕 ＋ 中央の不透明な面。**寸法は `layout.ts` の `CONFIRM_*` を
  `SaveLoadMenu` と共有している。⚠ **4つ目の形を作らないこと。**
  ⚠ **`CONFIRM_ON` の表を書き換えれば行為ごとに消せる**（#113 の受け口。設定画面はまだ無い）
- ⚠ **手際は「加工が速くなる」だけ**（#111 と同じ回で `canAttempt` を外した）。
  **着手を止める決まりは `fitsInToday`（その日のうちに終わるか）1つだけ。**
  ⚠ **改装に出している `1 → 1.7 → 3.0 → 5.3 → 9.2 → 16.0` は「作業精度」そのもので、
  全品がその倍率で速くなるわけではない**（簡単な品は `SPEED_FLOOR` に張り付く。
  **実測: 全段でバターは 180 → 45分 ＝ ×4**）。**実際の分数は工房の `時間` の列が正**
  ⚠ **この曲線そのものに PO が異議を出している** → **#112**
- ⚠ **`<input>` は左上基点で置く**（`domInput.ts` の `tryAddDom`）。**中心基点にしないこと。**
  Phaser の `DOMElement` は**中心へ寄せる幅と高さを1度だけ測って焼き付ける**ので、
  **できごとの窓・案内・セーブ画面が DOM の入れ物を隠している間に作った欄は 0×0 と測られ、
  指定点に「左上」が来る。**行商人は**その窓から開く**ので必ず踏む（#103）。
  ⚠ **欄の大きさは `createInput` に `box-sizing: border-box` があって初めて指定どおりになる**
  （`index.html` の `*` セレクタが効く。**無いと 160×24 が 150×18 で出る**）

⚠ **ここに残すのは「ファイルをまたぐ規則」と「知らないと設計を誤ること」だけ。**
**そのファイルを開けば目に入る注意は、コード側のコメントに置いてある。**

### 盤面と場所

- **在庫は1つ。**`DisplaySlot` は数量を持たない。**店＝船倉**で倉と売り場は同じ場所なので、
  区画は「どの品を、どこに、どの向きで出しているか」だけを表す。
  **「補充」は「もっと手に入れる」の意味**で、棚をクリックすると仕入れ／工房へ飛ぶ
- **仕入れ・工房・改装は「行く場所」**（#58）。中央の領域がまるごと入れ替わり、棚は**消える**。
  ⚠ **覆うのではなく消す**（**盤面は最大 x1473 まで伸び、キャラ帯の左端 `STRIP_L` 1470 を 3px はみ出す**）。
  ⚠ **はみ出す相手は場所の領域ではない**（`PLACE_R` はいま 1629）。
  **「場所の領域 976 を 6px」は #58 で広げる前の数で、2026-09-15 まで残っていた**（#117）
- ⚠ **「棚に手が出せるか」の判定は `GameScene.isShelfBlocked()` の1つだけ。**
  以前は同じ `||` の連鎖が7箇所に散らばり、**1箇所だけ強化メニューが抜けていた**
- ⚠ **破棄ゾーン（外周56px）から盤面をくり抜いている**（#72）。
  上端 y0〜56 と盤面の1行目 y8〜66 が重なるので、くり抜かないと
  **一番上の行へ動かすと棚から外れる。判定と描画の両方でくり抜くこと**（`rects.ts`）

### 画面をまたぐ規則（1箇所でも外すと壊れる）

- ⚠ **寸法は `src/ui/layout.ts` だけが持つ。写しを作らないこと**
- ⚠ **画面に出す文字を組み立てる側は、Phaser を読まないファイルに置く**
  （`layout.ts` ／ `money.ts` ／ `ShelfPresets.ts`）。
  **UI 側に置くと `layout.test.ts` が実物の文字列を測れない**
- ⚠ **`<input>` は `src/ui/domInput.ts` を通す**（工房の回数／一覧の検索／仕入れの個数）。
  打鍵をゲームへ漏らさない・DOM が無くても落ちない・消すときキー入力を戻す・ESC の逃げ道・
  **IME の変換中は絞り込みを走らせない**（#84）。**1箇所でも忘れると操作が効かなくなる**
- ⚠ **`<input>` を一覧と同じ入れ物に入れない。**打鍵のたびに作り直されてカーソルが飛ぶ。
  `SearchBox` ／ 工房の回数 ／ 型の名前は**どれも開くとき1度だけ**置いている
- ⚠ **一覧を「表」にしたら、はみ出す先は隣の列になる。**
  **列ごとに幅を測ること**（画面の左半分ぜんぶ、ではない）。
  **納品タブ（`DELIVERY_*`）と工房（`CRAFT_*`）が表。**見出しの字は**一覧の上に1回だけ**置く。
  ⚠ **1行になると、作れない／買えない理由を置く段が無くなる。**
  **理由はボタンの字に入れる**（商人タブ `BUY_REASON_FUNDS` ／ 工房 `CRAFT_REASON_*`）ので、
  **数を足さないこと**（`在庫が上限 400` の `400` はボタンから出る）
- ⚠ **通貨の組み立ては `src/ui/money.ts` の1本だけ**（`1,234レン`）。
  **`¥` の頃は16箇所に手書きで、桁区切りの有無すら揃っていなかった。**
  ⚠ **`レン` は全角2文字で `¥` より広い。金額を出す場所を増やすときは幅を測ること**
- ⚠ **目標額を書いてよいのは `GameService.GOAL_AMOUNT` だけ**（#73）。
  以前は**3箇所に別書き**で、進捗バーもチュートリアルも達成の幕も**実際と違うものを測っていた。**
  見せ方は `src/ui/goal.ts` の1本。
  ⚠ **`goal.test.ts` が「百万以上の数は `GameService` と `debug` にしか書けない」を検査する**
- ⚠ **画面に出す売値は `finalPrice(id, margin)`**（#76）。`salePrice()` は**素の売値**で、
  **強化の利益率が乗っていない。**⚠ **`derive.ts` に強化の段を持ち込まないこと**（品の性質ではない）
- ⚠ **画面に出す分数は `minutesFor` ／ `routeValues(..., skill)` を通す**（#53）。
  `recipe.durationMinutes` は**手際を掛ける前**で、**実際と最大3倍ずれる**
- ⚠ **「枠に収まるか」は `layout.test.ts` が `estTextWidth` で見ている。**
  字種ごとの係数は**実測値**（数字 0.64 ／ 区切りと空白 0.34 ／ それ以外の半角 0.84 ／ 全角 1.0）。
  ⚠ **見積もりが実測を下回ったらテストが嘘をつく。**その検査自体も入っている
- ⚠ **見出しの下の横線は `layout.ts` の `TITLE_RULE_Y`。**
  `PlaceFrame` が `TITLE_Y + 20` と直書きしていて、**帯を動かす側から見えず、一度壊した**
- ⚠ **キャラ絵の枠は `layout.ts` の `CHAR_ART_*`。**ボタン列の下端から引いてあるので、
  **右パネルにボタンを足すと枠が縮み、減らすと広がる。**
  **実測**: #9 で 200 → 155px ／ #24 で行商人が消えて **202px** ／ #96 で商人と改装が
  1つになって **249px**。⚠ **#15 の絵はこの大きさ。**足すときは #15 に断ること
- ⚠ **クリア後だけ、次の寄港地を選べる**（#7）。**クリア前の導出は1行も変わっていない** ——
  `WorldState.getLocation()` の分岐は `chosenIsland ?? ROUTE[portIndex]` **1箇所だけ。**
  ⚠ **選べるのは「次にどの島へ行くか」だけ。1寄港10日という周期は変えていない**
  （変えると #52・#61 の前提が全部動く）。
  ⚠ **選ばなければ今までどおりの順**（候補の先頭が順どおりの次）。
  ⚠ **窓で選ばせていない** —— **10日ごとに必ず窓が出るのは、既決「飛ばせない会話を周期的に
  発生させない」に当たる。**クリア後は**目標のバーがあった場所**が押せる行に入れ替わる
- ⚠ **窓を出すのは選択肢があるときだけ**（#24）。**読むだけの知らせはログへ。**
  **読むだけで窓を出すと、手が止まる回数がそのまま増える**（既決「飛ばせない会話を周期的に発生させない」）
- ⚠ **イベントの窓は「行く場所」（`PlaceFrame`）にしない。**あれは**棚を消してから**出る器。
  **選択肢1つのために店の風景を消さない。**⚠ **先例: `MessageLog` の遡りも同じ理由で場所にしなかった**
- ⚠ **イベントを1本足すのは `STORY_EVENTS` に1件書くだけ**（`components/progress/StoryEvents.ts`）。
  **発火（`StoryEventScheduler`）も `GameScene` も触らない。**
  結末に新しい語が要るときだけ `StoryOutcome` に足す —— **書き忘れると `tsc` が指す**（`never` の網羅）
- ⚠ **右パネルの「行く場所」は `取引` と `工房` の2つだけ**（#96）。
  **`取引` の中が 商人・改装・納品 の3タブ。**⚠ **タブは `PlaceFrame` の見出しの行に置いてある** ——
  **下に1行足すと一覧の上端が28px下がり、仕入れが8行→7行に減る**
- ⚠ **行商人はイベント。ボタン列には無い**（#90）。**最大1日1回・ランダム・営業中に来る。**
  ⚠ **積荷は来ない日も引いている**（来訪の側で引くとロード経由で引き直せるため）
- ⚠ **納品のミッションも「1日に1回しか引かない」で持っている**（#98）。
  **島を無くしたので、歯止めはこれだけになった** —— 元は「納品先で買えない品から選ぶ」だった。
  ⚠ **引いた日を `SaveData.orderDay` に積むこと。**積まないと**欲しい依頼が出るまでロードし直せる。**
  ⚠ **外れた日も、上限10件に達した日も「引いた」ことにする** ——
  しないと**外れた日だけ／1件納めた直後だけ**引き直せる（`DeliveryOrders.rollDaily`）
- ⚠ **知らせの重複抑止は `MessageLog` の側にある**（`info` の同じ文を続けて出さない）。
  **呼び出し側に印を持たせないこと。**⚠ **`sale` と `event` は抑止しない**（出来事は回数が情報）
- ⚠ **0件のとき「理由」を書き分ける。**「絞り込んだ結果」と「そもそも無い」は別物で、
  一緒にすると**序盤に何も知らないまま『揃っている』と言われる**（#48）

### 値と規則の構造（知らずに触ると符号が壊れる）

- **買値は全品同じ率**（`PURCHASE_RATE`）。これだけで
  `作る利益 − 転売利益 ＝ 加工利益 × 買値率` が成り立ち、
  **転売は薄利／作るほうが儲かる／深く作るほど取り分が増える**が同時に満たされる。
  ⚠ **tier ごとに率を分けないこと。**分けると符号が揃わなくなる
- **tier は「加工の深さ」であって「高級さ」ではない。**高級さは `贅沢さ` の軸
- **規則は適用範囲（`scope`）を宣言しない。**発火した効き目は**その品の tier で按分**され、
  一部が店全体へ、残りがその区画へ行く（tier1 は 90% が店全体／tier7 は 90% がその区画）。
  ⚠ **店全体ぶんは「置いてある区画数で割った平均」。**合計にすると効果の総量が区画数の
  **2乗**で伸びる（実測 13×10 で値段65倍）。**上限を足して抑えようとしないこと**
- ⚠ **取り合わせの規則は `SetRule` 1つに統一した**（#59 ／ 2026-09-14。`PairRule` ／
  `SignaturePairRule` ／ `SAME_ORIGIN_RULE` は無くなった）。**`SET_RULES` に1本足すだけで効く。**
  ⚠ **`members` は順序のある列で、効き目は先頭に当たった品にかかる。**
  **集合にすると R4「贅沢の隣に日用を置くと値が下がる」で日用品まで下がる。**
  ⚠ **`adjacent: false` が「離れていてよい」**（S1・S2）。**倍率は隣接より低くする**（PO 指定）——
  **位置を問わないボーナスを強くすると、置き方を考えなくてよくなる**
  ⚠ **`scope` を戻さないこと**（段3 で廃止。適用範囲は品の tier から按分）
- ⚠ **入荷解禁は `stockedBy` で絞って「どれか1本でも通るか」を見る**（#59）。
  **`UNLOCK_RULES` に1本足せば評価器を触らずに効く。**以前は `r.id === 'U1'` と ID で名指ししていて、
  **U3 を足しても無視された**
- ⚠ **規則が見る「産地」は、品に書いてある産地ではない**（#37 ／ 2026-09-14）。
  **条件言語の `産地` 述語と R5・D2 は `derive.ts` の `originReach()` を通る** ——
  **材料をすべて遡って行き着く島が1つに定まればその島、2島以上混ざれば `なし`。**
  ⚠ **書いてある産地をそのまま読むのは、商人が何を並べるか（U3・U4）と産地割引だけ。**
  **どちらも評価器の側に置いてある。**
  ⚠ **`産地` を見る規則を新しく書くと、自動的に「遡ったほう」を見る。**
  **実測**: 当たる品は **48 → 74 / 161品**（加工品106品のうち26品が1島に定まる）。
  ⚠ **「材料に1つでも入っていれば」にすると 154/161 になり、
  「並べ方で客足が動く幅は最大2倍」が常時かかる下駄に変わる**
- ⚠ **`stockedBy`（`rules.ts` の `StockedBy`）を評価器は読んでいない。**
  **並ぶ先ごとに関数を1本ずつ持っている**（`stockedByIslandMerchant` ／ `stockedByPeddler`）。
  ⚠ **規則データに `stockedBy` を足しても効かない。足すと規則が嘘になる**
- ⚠ **行商人の割増は `PeddlerStock` 側で掛ける。**
  **`purchasePrice()` に引数を足さないこと** —— `derive.ts` の「その島の中で率は2つだけ」が壊れる
- ⚠ **客に売れた経路以外の入金は `EconomyManager.addIncome()` を通す**（#28）。
  `addRevenue()` が積む `totalRevenue` は「**客に売れた額**」であって「入った金」ではない
- ⚠ **`materials.ts` の素材展開は `outputQuantity` で割る。**92本中64本が2個以上なので、
  割らないと要る素材を**段ごとに最大4倍**に見積もる（#33 で一度やって #23 で直した）

### 時間

- ⚠ **加工中は客が1人も来ない**（`skipMinutes` は `TIME_MINUTE_PASSED` を出さない）。
  **営業時間に食い込む加工は売上を失う**（実測 **2.6円/分**・線形）。
  ⚠ **無料枠は240分**（`fitsInToday` が24:00跨ぎを禁じ、**夜と朝がつながっていない**）
- ⚠ **航海日は無い。`atSea` は `src/` から消えた**（#74）。
  戻すなら `DAYS_AT_SEA = 1` と導出の復活だが、**周期が 10 → 11日になり
  `daysUntilReturn()` が 40 → 44日になる**ので #61 の対象が増える
- ⚠ **納品の個数は tier で割る**（#85。tier1:10 ／ tier7:1）。
  **一律だと tier7 は作るのに24日ぶんかかり、滞在10日に入らない**

### 品と世界

- **品は 161、レシピは 106、tier は 7 まで**（素材55／加工品106）。
  ⚠ **一覧の正は `src/taxonomy/items.ts` ／ `recipes.ts`。**文書は理由だけを持つ
- ⚠ **品には `display.reading`（ひらがなの読み）が必須**（#65）。欠けると検索が黙って外れる
  （不変条件テストで縛ってある）。**工房も同じ1箇所を通る**ので、レシピ側には書かない
- ⚠ **品を作る前に [island-goods.md §0](inception/worldbuilding/island-goods.md) を読む。**
  **素材も加工品も。**それまで線が散っていて、**#86 で実際に踏んだ**
- ⚠ **旬を持たない品にも、産地を付けてよくなった**（#87 ／ **`island-goods.md` §2-B** 新設）。
  **土地から掘り出すものは PO が島を指名する。**先例は **鉄鉱石＝リナツィア。**
  ⚠ **`なし` に落とすと規則 U4 でどの島にも並び、その系統から航海の理由が消える。**
  ⚠ **数（「特産が少ない島」など）を手続きとして文書に書かないこと** ——
  **測り方で答えが割れ**（実測: 素材の数では3島が同点／下流の加工品ではリナツィア／
  その島だけに依る加工品ではミフユリア）、**品を足すたびに答えが動く**（§2-A と同じ失敗）。
  ⚠ **粘土を足さなかった理由は、この節で外れている**（→ #89）
- ⚠ **武器は `道具` の品。タグも主種類も足していない**（#87）。**「武器である」ことは品名が言っている**
- ⚠ **魔法はある／異種族はいない／戦争はない**（武器そのものは在ってよい。PO 判断 2026-09-13）。
  ⚠ **魔法を価値の出どころにしないこと** —— **売値 ＝ 材料費 ＋ 加工利益**（tier1 は 基準値 × 贅沢さの倍率）。
  **加工利益 ＝ 5.6 × (所要分 ÷ 出力数)^0.6 × 贅沢さ係数**（日用0.9／上等1.1／贅沢1.7）。
  **値打ちの出どころは「材料」「所要分」「贅沢さ」の3つだけ。**
  ⚠ **「魔法だから高い」という4つ目を作らないこと。**作ると**軸が1本増え、導出が全部その上に乗る**
- ⚠ **仕入れ画面は「買えるもの ＋ もうすぐ買えるもの」**（#66・PO 判断）。
  もうすぐ買える行は**買う部品をそもそも作らない**ので `buy()` から手が届かない。
  ⚠ **しきい値（100）を画面にもテストにも書き写さない。**規則を評価して残りを探す

### 人物の見た目

- **ネームド8人の全身図がある**（`inception/worldbuilding/art/characters/`、8枚 約17MB）。
  **依頼書は `inception/worldbuilding/prompts/characters/`。**外見の正は
  [characters.md](inception/worldbuilding/characters.md)、主人公だけ
  [char-visual.md](inception/worldbuilding/char-visual.md)（案F で確定）
- ⚠ **服の色と留め具に制約は無い**（PO 判断 2026-09-13 ／ #88）。
  **島の人も染めた服を着るし、金属の留め具も使う。**
- ⚠ **`materials.md` ／ `crafted-goods.md` から世界の制約を導かないこと。**
  あの2つは**商品として扱うものの一覧**であって、世界の目録ではない。
  **「無い」と書いてあるのは「商品として扱っていない」という意味**
- **主人公の髪・瞳は濃茶で確定。叔母テレナも同じ濃茶。**
  ⚠ **見分けは髪色ではなくシルエット**（大きな帽子・肩丈の外はね・丸く広がるスカート）に載せる
- ⚠ **色を人物の描き分けの軸に使わないこと。**島の人も染めた服を着るので色では差が付かない。
  **差が付くのは、質感の混ざり方・装飾の密度・素材がいくつの島から来ているか**（`attire.md` §3）
- **画像は通常の blob で入れる。LFS は入れない**（`.git` 62MB。GitHub の警告線まで余裕がある）。
  ⚠ **差分・サイズ違いを積み始めるときに再検討する**

---

## 進め方の決まり

- ⚠ **値を解かない。構造だけ決める**（PO 決定）。
  **後回しにできるのは値**（倍率・費用・確率・日数・上限）、
  **できないのは構造**（どの効き目を使うか／どこに差を置くか／何を上限にするか）。
  **仮置きの値はすべて #61 に集める。**→ 規則の全文は
  [issue-grouping.md §4](inception/plans/issue-grouping.md)
- **UI に触ったら、確認してほしいことを #47 の本文に1行足す。**
  テスト・型・ビルドが通っていても、画面の不具合はそこでしか出ない。
  ⚠ **確認は1件ずつ頼まない。**#47 にためて**最後にまとめて**見てもらう（PO 決定）
- ⚠ **先に言葉を置くと、判断がその言葉に寄る。**
  **消す／直す理由を書くときは、それが本当にその箇所に当たっているかを確かめる。**
  束M で誤った理由で文言を消し、**ペルソナ4人全員に指摘されて戻した。**
  **だから #79 には言い回しの案を書かない**（PO が指示する）
- **サブへ委任するときは、前提を1つ残らず依頼書に書く。**
  先例: [economy-rebalance-handoff-2.md](construction/plans/economy-rebalance-handoff-2.md)
- **確認用の道具**（`src/debug/`）: `M` 金+100万 ／ `N` 1日 ／ `B` 10日 ／
  `I` この島の品を各100 ／ `O` 全品を各100 ／ **`P` 行商人を呼ぶ** ／ `U` 強化を全系統1段。
  **出荷前に丸ごと外す**（#51）。外すのは `src/debug/` と `GameScene` の呼び出し1箇所、
  ⚠ **それと `GameScene.raiseStoryEvent()`**（確認用からしか呼んでいない）

---

## ペルソナが挙げた危うい点

→ [persona-review.md](inception/design-log/persona-review.md)

**①拡張がパズルを殺す ②資源が実質お金だけ ③200日の中身が示されていない。**
**①は段5（高tierほど大きい升）で、②は段3〜5で答えた。**⚠ **残っているのは ③。**

---

## これまでの経緯

→ [history.md](history.md) ／ [audit-archive.md](audit-archive.md)

⚠ **経緯を現在地として読まないこと。**あちらには、いまはもう成り立たない数字が残っている
（航海日があった頃の周期、買値率、目標額、それらの上で測った日数など）。
**いま何がどうなっているかは、この文書だけを見ること。**
