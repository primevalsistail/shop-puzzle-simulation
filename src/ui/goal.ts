/**
 * 目標（クリア条件）を**画面に出すための1箇所**（#73）。
 *
 * ## なぜこのファイルがあるか
 *
 * 目標を出す画面は3つある —— **進捗バー**（`HUD`）／**チュートリアル**（`Tutorial`）／
 * **目標達成の幕**（`GameScene`）。以前はこの3つが**それぞれ別の数字を持っていて**、
 * 進捗バーとチュートリアルは `累計売上 / 100万`、実際のクリア判定は `所持金 / 1000万` だった。
 * **軸も桁も違う。**バーが 100% になってもクリアしない。
 *
 * ⚠ **額の正は `GameService.GOAL_AMOUNT` の1つだけ。**ここはそれを読んで文字にするだけで、
 *   自分の数字を持たない。**別書きが増えていないかは `goal.test.ts` が見ている。**
 *
 * ⚠ **Phaser を読まない。**読むと `goal.test.ts` が実物の文字列を測れなくなる。
 */

import { GOAL_AMOUNT } from '../services/GameService.js'
import { money } from './money.js'

/**
 * チュートリアル最後の行。⚠ **初日に必ず見る画面**なので、ここが実際の条件と違うと
 * **遊び始めた瞬間から嘘をつく**ことになる。
 */
export const GOAL_TUTORIAL_LINE = `目標: 所持金 ${money(GOAL_AMOUNT)} を達成しよう！`

/*
 * ⚠ **`goalReachedLine(currentMoney)` は 2026-09-15 に消した**（#97）。
 *   **幕の入口が「目標額に届く」から「商船を買う」に変わった**ので、
 *   **幕が出る時点の所持金は、払った目標額ぶん減っている。**
 *   出していたのは所持金なので、**そのまま残すと `所持金 0レン` が出る。**
 *   代わりは下の `shipBoughtLine()`。
 *   ⚠ **また所持金で幕を出すなら、ここへ戻して `goal.test.ts` に検査も戻すこと。**
 */

/**
 * **商船の値段**（#97）。⚠ **目標額と同じ**（決定 2026-09-15 ／
 * `sessions/questions-96-97.md:52`）。**額そのものは書かない** —— `GOAL_AMOUNT` は
 * 1,000万から 2,000,000 に変わっており、**写した数は必ず古くなる。**
 *
 * ⚠ **ここも自分の数を持たない。**`GOAL_AMOUNT` を引くだけ。
 *   **額の正は `GameService.GOAL_AMOUNT` の1つだけ**という #73 の形をそのまま使う。
 * ⚠ **エンディングの入口はここ**（#97）。**所持金が目標額に届いても幕は出ない。**
 *   届いた金で**商船を買った**ときに出る。
 */
export const SHIP_COST = GOAL_AMOUNT

/**
 * 商船を買ったときの幕に出す1行。
 *
 * ⚠ **`所持金` は出せない。**買った直後の所持金は**目標額ぶん減っている**ので、
 *   ここに所持金を出すと**エンディングの幕に `所持金 0レン` が出る。**
 * ⚠ **文言は PO の領分**（#79）。**いまのは仮。**
 */
export function shipBoughtLine(): string {
  return `商船 ${money(SHIP_COST)}`
}
