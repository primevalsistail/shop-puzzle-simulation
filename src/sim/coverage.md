# 測る道具が見ているもの・見ていないもの

⚠ **数字を出す前と、出した数字で何かを決める前に、この表を上から読む。**
**ここに `❌` があるまま出した到達日は、その仕組みが無い世界の到達日である。**

**なぜ要るか**: 2026-09-15、`深い品を作る` が 232〜236日で帯に入らず、
**材料が揃わないのが原因**だと報告した。**原因は道具の側だった** ——
**行商人**（島で採れない品を割高で運ぶ。まさにこの問題のために作った仕組み・#34）を、
**方針が一度も使っていなかった。**`SimWorld` は毎日積荷を作っていたので、
**「載っている」だけ見て済ませると気づけない。**

---

## ⚠ 落とし穴は「載っているか」ではなく「方針が触れるか」

**列が2つあるのはそのため。**`SimWorld` に仕組みがあっても、
**`SimContext` に窓が無ければ、方針はそれが存在しないものとして振る舞う。**

| | 載 | 触 |
|---|---|---|
| **載** | `SimWorld` がその仕組みを動かしているか | |
| **触** | | **方針（`policies.ts`）がそれを見て・使って判断できるか**。⚠ **自動で起きるだけのものは `自動` と書く** |

---

## お金に効く仕組み（ここが欠けると到達日が変わる）

| 仕組み | 置き場 | 載 | 触 | |
|---|---|---|---|---|
| **時間・営業時間** | `TimeManager` | ✅ | 自動 | |
| **来店客** | `CustomerSimulator` | ✅ | 自動 | 年ごろ・好みも込み |
| **1分ごとの売買** | `GameService` | ✅ | 自動 | |
| **在庫** | `Inventory` | ✅ | ✅ | 上限999・`hasEverHeld` も |
| **盤面と配置ボーナス** | `FloorGrid` ／ `SET_RULES` | ✅ | ✅ | `arrange` で置き方を変えられる |
| **島の商人の品揃え（U1・U2）** | `merchantListing` | ✅ | ✅ | `ctx.stocked` |
| **産地割引** | `purchasePriceOf(id, island)` | ✅ | ✅ | |
| **島の需要** | `DEMAND_TABLE` | ✅ | ✅ | |
| **加工** | `CraftingSystem` | ✅ | ✅ | 営業時間を削るところも |
| **レシピ解禁** | `RecipeUnlocks` | ✅ | ✅ | 種まきの1個買いで開かせている |
| **改装（強化）** | `Upgrades` | ✅ | 自動 | `--upgrade-ratio` で買う |
| **島が10日で変わる** | `WorldState` | ✅ | 自動 | 順路は固定 |
| **行商人** | `PeddlerStock` | ✅ | ✅ | **2026-09-15 に繋いだ**（`b07e3fa` の次）。<br>`BuyOrder.from = '行商人'` で買う。**歯止め4つはそのまま**（10種類・次の寄港地の産は積まない・解禁は同じ・割高） |
| **⚠ 納品の依頼** | `DeliveryOrders` | ✅ | **自動のみ** | **配れるものを配るだけ。**<br>**依頼を狙って仕入れる方針が書けない。**報酬は売値の3倍（`ORDER_REWARD_RATE`）<br>⚠ **全方針に同じだけ効くので、比べるぶんには歪まない。**<br>**ただし到達日はどれも実際より遅く出ている** |
| **棚を埋めきる** | `SimContext.shelfKinds` | ✅ | ✅ | **2026-09-15 に直した**（下記の輪） |

## お金に効かないもの（載せなくてよい。**理由も書く**）

| 仕組み | なぜ載せないか |
|---|---|
| **型（`ShelfPresets`）** | 並べ直す手間を減らすだけ。**同じ盤面に行き着く** |
| **できごと（`StoryEvents`）** | **金も物も動かさない**（副作用を持たないことを確認済み 2026-09-15） |
| **セーブ・ロード** | 遊びの結果を変えない |
| **自由航行** | **エンディング後**。帯（160〜200日）の外 |
| **画面まわり全部** | HUD・メニュー・絵・音 |

---

## 足すときの決まり

- ⚠ **仕組みを足したら、この表に1行足す。**`load` と `save` に足し忘れると静かに消えるのと同じで、
  **ここに書き忘れると静かに測り落とす**
- ⚠ **`触` を `自動` にしてよいのは、プレイヤーが選べないものだけ。**
  **選べるのに自動にすると、「その選択が下手な人」を測ることになる**
- ⚠ **`❌` を直さずに数字を出さない。**出すなら**何が無い世界の数字か**を必ず添える

---

## ⚠ 見落としの2つ目の形 —— 自分に食いつく輪

**「載っているか」「触れるか」の両方が ✅ でも、まだ落とし穴がある。**
2026-09-15、`深い品を作る` が遅い理由をもう一段掘って見つけた:

**転売品を「前の日に並んだ区画の数だけ」仕入れる作りだった。**
**区画が少ない方針は少ししか仕入れず、仕入れないから区画が増えない。**

| 方針 | 区画 | 升 | 棚の容量 |
|---|---|---|---|
| 浅い品だけ作る | **51** | **109** | 130 |
| 深い品を作る | **23** | **58** | 130 |

**深い品を作るは、棚の 130升 のうち 72升 を空けたまま320日回っていた。**
**空いた棚を見て買い足す**のは遊ぶ人なら誰でもすることで、**できないのは道具の欠けである。**
→ **`棚の総升数 ÷ 並べた品の平均升数` で見積もるように直した。**

⚠ **見つけかた**: **方針どうしで内訳を並べ、桁が違う列を探す。**
**「区画20 / 升46」は当時「そういう遊び方だから」で流していた。**
**流さずに「なぜこの方針だけ半分なのか」と訊けば、そこに輪がある。**
